{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"pydynantic","text":"<p>Single-table design for DynamoDB in Python, with typed entities on top of Pydantic v2.</p> <p><code>pydynantic</code> is the Pythonic answer to JavaScript's ElectroDB and DynamoDB-Toolbox: model many entities in a single DynamoDB table, compose keys from attributes, declare named &amp; typed access patterns, and never hand-write an <code>UpdateExpression</code> again.</p> <ul> <li>Declarative entities built on Pydantic v2 (validation, defaults, nested models).</li> <li>Automatic key composition from templates like <code>\"ORG#{org_id}\"</code> \u2014 primary keys and GSIs.</li> <li>Typed, named access patterns instead of raw expressions.</li> <li>Transparent marshalling for <code>str</code>, <code>int</code>, <code>float</code>, <code>Decimal</code>, <code>bool</code>, <code>bytes</code>,   <code>datetime</code>, <code>date</code>, <code>UUID</code>, <code>Enum</code>, <code>list</code>, <code>dict</code>, <code>set</code>, and nested Pydantic models.</li> <li>Single-table primitives: collections, batch get/write, transactions, optimistic locking, cursor pagination.</li> <li>Type-safe: ships <code>py.typed</code>, passes <code>mypy --strict</code>.</li> </ul> <p>Note</p> <p>The library never opens its own connections \u2014 you inject a boto3 client, which keeps credentials, region, endpoint, and testing fully in your control.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install pydynantic\n</code></pre> <p>Requires Python 3.10+ and <code>boto3</code> / <code>pydantic&gt;=2</code>.</p>"},{"location":"#quick-start","title":"Quick start","text":"<pre><code>import boto3\nfrom datetime import datetime, timezone\nfrom pydynantic import Table, Entity, Field, key, F\n\n# 1. Describe the physical table (one instance per real table).\ntable = Table(\n    name=\"app-prod\",\n    pk=\"PK\",\n    sk=\"SK\",\n    indexes={\"GSI1\": {\"pk\": \"GSI1PK\", \"sk\": \"GSI1SK\"}},\n    client=boto3.client(\"dynamodb\"),   # injected; testable\n)\n\n# 2. Declare entities. Attributes use plain Pydantic syntax; keys are templates.\nclass User(Entity, table=table, name=\"user\"):\n    user_id: str\n    org_id: str\n    email: str\n    name: str\n    status: str = \"active\"\n    login_count: int = 0\n    created_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))\n\n    class Meta:\n        primary = key(pk=\"ORG#{org_id}\", sk=\"USER#{user_id}\")\n        by_email = key(index=\"GSI1\", pk=\"EMAIL#{email}\", sk=\"USER#{user_id}\")\n\n# 3. CRUD \u2014 no expressions by hand.\nUser.put(User(user_id=\"u1\", org_id=\"acme\", email=\"a@x.com\", name=\"Ana\"))\n\nuser = User.get(org_id=\"acme\", user_id=\"u1\")          # -&gt; User | None\n\nUser.update(\n    org_id=\"acme\", user_id=\"u1\",\n    set={\"name\": \"Ana B.\", \"status\": \"inactive\"},\n    add={\"login_count\": 1},\n)\n\nUser.delete(org_id=\"acme\", user_id=\"u1\")\n</code></pre>"},{"location":"#where-to-go-next","title":"Where to go next","text":"<ul> <li>New to single-table modelling? Start with Single-table design.</li> <li>Ready to model? See Entities &amp; keys, then CRUD   and Queries.</li> <li>Need patterns? Browse the Recipes cookbook.</li> <li>Coming from JavaScript? See Migrating from ElectroDB or   Migrating from DynamoDB-Toolbox.</li> <li>The exhaustive symbol list lives in the API reference.</li> </ul>"},{"location":"#scope","title":"Scope","text":"<p>In scope (v1): single-table modelling, keys, CRUD, queries, filters/conditions, pagination, batch, transactions, collections, optimistic locking. Synchronous only.</p> <p>Out of scope: table/infra provisioning (use CDK/Terraform), data migrations, multi-table joins, async (planned for v2 on <code>aioboto3</code>).</p>"},{"location":"policies/","title":"Project policies","text":"<p>This page documents the stability, deprecation, Python-support, and release-tag commitments that <code>pydynantic</code> makes from 1.0.0 onward. The goal is predictability: you should be able to pin a major version and upgrade minors/patches without surprises.</p>"},{"location":"policies/#semantic-versioning","title":"Semantic versioning","text":"<p><code>pydynantic</code> follows Semantic Versioning 2.0.0 once 1.0.0 ships. Versions are <code>MAJOR.MINOR.PATCH</code>:</p> <ul> <li>MAJOR \u2014 incompatible (breaking) changes to the public API.</li> <li>MINOR \u2014 new, backwards-compatible functionality (including new deprecations).</li> <li>PATCH \u2014 backwards-compatible bug fixes.</li> </ul>"},{"location":"policies/#what-the-public-api-means","title":"What \"the public API\" means","text":"<p>The public API is everything exported from the top-level <code>pydynantic</code> package \u2014 that is, every name in <code>pydynantic.__all__</code>. This includes classes you instantiate (<code>Table</code>, <code>Entity</code>, <code>Transaction</code>), factory functions (<code>key</code>, <code>transaction</code>, <code>encode_cursor</code>/<code>decode_cursor</code>), expression helpers (<code>F</code>, <code>attr_exists</code>, <code>attr_not_exists</code>), the attribute helpers (<code>version_attr</code>, <code>ttl_attr</code>, <code>created_at_attr</code>, <code>updated_at_attr</code>), the re-exported <code>Field</code>, and the exception hierarchy rooted at <code>PydynanticError</code>.</p> <p>Builder and definition types such as <code>QueryBuilder</code>, <code>ScanBuilder</code>, <code>QueryNamespace</code>, <code>KeyDefinition</code>, <code>Condition</code>, and <code>OperationHook</code> are part of the public surface too: they are the return and parameter types of public methods (e.g. <code>Entity.scan()</code> returns a <code>ScanBuilder</code>, <code>key()</code> returns a <code>KeyDefinition</code>, <code>F(...)</code> builds a <code>Condition</code>). They are intended to be referenced in type annotations and isinstance checks, and are covered by the same stability guarantee.</p> <p>Not part of the public API, and changeable at any time without a major bump:</p> <ul> <li>Any name prefixed with an underscore (<code>_Comparison</code>, <code>_ClientProxy</code>, \u2026).</li> <li>Any module not re-exported from the top-level package (importing from   <code>pydynantic.query</code>, <code>pydynantic.expressions</code>, etc. is unsupported).</li> <li>The string value of <code>__version__</code> as a parseable contract beyond SemVer ordering.</li> <li>Behaviour explicitly documented as \"internal\", \"experimental\", or \"subject to change\".</li> </ul> <p>We commit to introducing breaking changes to the public API only in a new major release.</p>"},{"location":"policies/#deprecation-process","title":"Deprecation process","text":"<p>Nothing in the public API is removed or changed incompatibly without a deprecation period. The process for retiring a symbol, parameter, or behaviour is:</p> <ol> <li>Announce in a minor release. The deprecated symbol/parameter keeps working and    emits a <code>DeprecationWarning</code> (via <code>warnings.warn(..., DeprecationWarning,    stacklevel=2)</code>) the first time it is used. The warning message names the    replacement and the earliest version in which removal may occur.</li> <li>Document it. The deprecation is recorded in the    CHANGELOG    under a Deprecated heading for that release, and the symbol's docstring is    annotated (e.g. with a <code>.. deprecated::</code> / \"Deprecated since\" note).</li> <li>Honour a minimum grace period. A deprecated API remains available, with its    warning, for at least one full minor release before it becomes eligible for    removal.</li> <li>Remove only in the next major. Actual removal of a deprecated public API happens    in the next major version, never in a minor or patch. The removal is listed in    the CHANGELOG under a Removed heading.</li> </ol> <p>This gives downstream code a clear, machine-detectable upgrade path: run your test suite with <code>-W error::DeprecationWarning</code> to surface anything that will break in the next major.</p>"},{"location":"policies/#supported-python-versions","title":"Supported Python versions","text":"<p><code>pydynantic</code> supports the CPython versions exercised in CI:</p> Status Versions Supported 3.10, 3.11, 3.12, 3.13 <p>Policy for changing this set:</p> <ul> <li>Adding a newly released Python version is a backwards-compatible change and may   land in a minor release once it is green in CI.</li> <li>Dropping a Python version (typically once it reaches   end-of-life) is a breaking change and is done   only in a major release. The drop is announced in the CHANGELOG under   Deprecated/Removed and the <code>requires-python</code> constraint is updated accordingly.</li> </ul>"},{"location":"policies/#release-tag-naming","title":"Release-tag naming","text":"<p>Releases are cut from Git tags. The standard, going forward, is a bare version tag:</p> <pre><code>X.Y.Z          e.g.  1.0.0, 1.2.3\n</code></pre> <p>Bare tags are the primary trigger of the release workflow that publishes to PyPI.</p> <p>Historical exception</p> <p>The <code>0.1.0</code> release was tagged <code>v0.1.0</code> (with a leading <code>v</code>). That tag is preserved for history, but all releases from now on use bare <code>X.Y.Z</code> tags to match the release workflow. Do not create <code>v</code>-prefixed tags.</p>"},{"location":"recipes/","title":"Recipes","text":"<p>A cookbook of patterns built from the primitives in the guide. Each recipe assumes a configured <code>table</code> and the imports it shows.</p>"},{"location":"recipes/#status-index-overloading","title":"Status-index overloading","text":"<p>Serve \"list by status\" from an overloaded GSI without a separate table. The GSI partition is the status value, so a query returns just that status's items \u2014 and the index is sparse: only entities defining the key appear in it.</p> <pre><code>from pydynantic import Entity, Field, key\n\nclass Ticket(Entity, table=table, name=\"ticket\"):\n    ticket_id: str\n    project_id: str\n    status: str = \"open\"           # open | in_progress | closed\n    priority: int = 3\n\n    class Meta:\n        primary = key(pk=\"PROJECT#{project_id}\", sk=\"TICKET#{ticket_id}\")\n        by_status = key(index=\"GSI1\", pk=\"STATUS#{status}\", sk=\"TICKET#{ticket_id}\")\n\n# All open tickets across every project, one query:\nopen_tickets = Ticket.query.by_status(status=\"open\").all()\n</code></pre> <p>When a ticket's status changes, update it through <code>set=</code> and <code>pydynantic</code> recomputes the GSI key automatically:</p> <pre><code>Ticket.update(project_id=\"p1\", ticket_id=\"t1\", set={\"status\": \"closed\"})\n</code></pre>"},{"location":"recipes/#pagination-in-an-http-handler","title":"Pagination in an HTTP handler","text":"<p>Return one page per request and pass the opaque cursor through the query string. The cursor is URL-safe, so no encoding gymnastics.</p> <pre><code>def list_users(org_id: str, cursor: str | None = None):\n    page = User.query.primary(org_id=org_id).limit(25).page(cursor=cursor)\n    return {\n        \"items\": [u.model_dump() for u in page.items],\n        \"next_cursor\": page.cursor,    # None when exhausted; echo back to fetch more\n    }\n</code></pre> <p>A stale or tampered cursor raises <code>PydynanticError</code>; map it to a <code>400 Bad Request</code>.</p>"},{"location":"recipes/#idempotent-create","title":"Idempotent create","text":"<p>Make \"create if absent\" safe to retry by guarding on the primary key's absence. A retry that loses the race fails with a typed error instead of overwriting.</p> <pre><code>from pydynantic import attr_not_exists, ConditionCheckFailedError\n\ndef create_user_once(user: User) -&gt; bool:\n    try:\n        User.put(user, condition=attr_not_exists(\"PK\"))\n        return True            # we created it\n    except ConditionCheckFailedError:\n        return False           # already existed; safe no-op\n</code></pre>"},{"location":"recipes/#atomic-counters","title":"Atomic counters","text":"<p>Use <code>add=</code> for server-side, race-free numeric increments \u2014 no read-modify-write.</p> <pre><code># Bump a login counter atomically and read back the new total:\nupdated = User.update(org_id=\"acme\", user_id=\"u1\", add={\"login_count\": 1})\nprint(updated.login_count)\n\n# Decrement (negative delta), guarded so it never goes below zero:\nfrom pydynantic import F\nUser.update(\n    org_id=\"acme\", user_id=\"u1\",\n    add={\"credits\": -1},\n    condition=F(\"credits\") &gt;= 1,\n)\n</code></pre>"},{"location":"recipes/#soft-delete-via-ttl","title":"Soft-delete via TTL","text":"<p>Mark an item for expiry instead of deleting it, and hide it immediately with a filter (DynamoDB removes expired items lazily, typically within 48 hours).</p> <pre><code>from datetime import datetime, timedelta, timezone\nfrom pydynantic import Entity, key, ttl_attr, F\n\nclass Note(Entity, table=table, name=\"note\"):\n    note_id: str\n    user_id: str\n    body: str\n    expires_at: datetime | None = ttl_attr()\n\n    class Meta:\n        primary = key(pk=\"USER#{user_id}\", sk=\"NOTE#{note_id}\")\n\n# Soft-delete: schedule expiry 30 days out.\nNote.update(\n    user_id=\"u1\", note_id=\"n1\",\n    set={\"expires_at\": datetime.now(timezone.utc) + timedelta(days=30)},\n)\n\n# Hide soft-deleted notes right away (don't wait for DynamoDB's sweep):\nnow = datetime.now(timezone.utc)\nlive = (Note.query.primary(user_id=\"u1\")\n            .filter(F(\"expires_at\").not_exists() | (F(\"expires_at\") &gt; now))\n            .all())\n</code></pre>"},{"location":"recipes/#multi-entity-partition-reads","title":"Multi-entity partition reads","text":"<p>Load several entity types from one partition in a single query with a collection.</p> <pre><code>from pydynantic import Collection\n\nclass OrgData(Collection):\n    members = [User, Membership, Invoice]   # all keyed PK = ORG#{org_id}\n\nresult = OrgData.query(org_id=\"acme\").all()\ndashboard = {\n    \"users\": result.users,\n    \"memberships\": result.memberships,\n    \"invoices\": result.invoices,\n}\n</code></pre>"},{"location":"concepts/single-table-design/","title":"Single-table design","text":"<p>If you come from relational databases, the natural instinct is one table per entity: a <code>users</code> table, an <code>orders</code> table, a <code>memberships</code> table. DynamoDB rewards the opposite instinct. Single-table design (STD) puts many entity types in one physical table and uses the key schema to keep related items physically close, so a single <code>Query</code> returns everything an access pattern needs without joins.</p> <p><code>pydynantic</code> exists to make STD ergonomic in Python: you keep modelling typed entities, and the library handles the key gymnastics underneath.</p>"},{"location":"concepts/single-table-design/#one-table-generic-keys","title":"One table, generic keys","text":"<p>A DynamoDB table has a partition key (PK) and an optional sort key (SK). In STD these are deliberately generic \u2014 usually named <code>PK</code> and <code>SK</code> \u2014 because they will hold composed string values for many different entity types, not a single natural id.</p> <pre><code>PK                     SK                  __entity__   attributes...\nORG#acme               USER#u1             user         email=a@x.com, name=Ana\nORG#acme               USER#u2             user         email=b@x.com, name=Bob\nORG#acme               INVOICE#2024-001    invoice      total=120.00\nORG#acme               MEMBERSHIP#u1       membership   role=admin\n</code></pre> <p>Three different entity types share the partition <code>ORG#acme</code>. One query on that partition returns the org's users, invoices, and memberships together \u2014 an item collection.</p> <p>In <code>pydynantic</code>, those composed values are never typed by hand. You declare templates and the library renders them:</p> <pre><code>class Meta:\n    primary = key(pk=\"ORG#{org_id}\", sk=\"USER#{user_id}\")\n</code></pre> <p><code>org_id=\"acme\"</code>, <code>user_id=\"u1\"</code> \u2192 <code>PK=\"ORG#acme\"</code>, <code>SK=\"USER#u1\"</code>.</p>"},{"location":"concepts/single-table-design/#item-collections","title":"Item collections","text":"<p>An item collection is the set of items that share the same partition key. Because DynamoDB stores them together and a <code>Query</code> reads a contiguous slice of one partition, fetching a collection is a single, cheap, sorted read.</p> <p>The sort key gives you range power within a collection:</p> <ul> <li><code>begins_with(\"USER#\")</code> \u2014 only the users in the org.</li> <li><code>between(\"INVOICE#2024-01\", \"INVOICE#2024-12\")</code> \u2014 a date range, because ISO strings   sort lexicographically.</li> </ul> <p>See the Collections guide for retrieving several entity types from one partition in one call.</p>"},{"location":"concepts/single-table-design/#gsi-overloading","title":"GSI overloading","text":"<p>A Global Secondary Index (GSI) is a second key schema over the same items, letting you query by attributes that are not the primary key. In STD, GSIs are overloaded: the same physical index (e.g. <code>GSI1PK</code> / <code>GSI1SK</code>) serves different access patterns for different entities.</p> <pre><code>class User(Entity, table=table, name=\"user\"):\n    user_id: str\n    org_id: str\n    email: str\n\n    class Meta:\n        primary = key(pk=\"ORG#{org_id}\", sk=\"USER#{user_id}\")\n        by_email = key(index=\"GSI1\", pk=\"EMAIL#{email}\", sk=\"USER#{user_id}\")\n</code></pre> <p><code>by_email</code> writes <code>GSI1PK=\"EMAIL#a@x.com\"</code>, so you can look a user up by email without knowing their org. A different entity can reuse <code>GSI1</code> with a totally different template (for instance <code>STATUS#{status}</code>), and each entity's items only appear under the patterns it defines.</p> <p>Sparse indexes</p> <p>If an entity doesn't define a key for a given index, its items simply don't carry that index's attributes \u2014 so they never show up in that index. Overloading plus sparseness is how one GSI cleanly serves many unrelated queries.</p>"},{"location":"concepts/single-table-design/#access-pattern-first-modelling","title":"Access-pattern-first modelling","text":"<p>The relational workflow is: model the entities, normalize, then write whatever queries you need. DynamoDB inverts this. You list the access patterns first, then design keys that satisfy them with single-partition reads.</p> <p>A typical worksheet:</p> Access pattern Key shape Get a user by id within an org PK <code>ORG#{org_id}</code>, SK <code>USER#{user_id}</code> List all users in an org PK <code>ORG#{org_id}</code>, SK <code>begins_with(\"USER#\")</code> Find a user by email GSI1 PK <code>EMAIL#{email}</code> List active users GSI1 PK <code>STATUS#{status}</code> <p><code>pydynantic</code> maps each row to a named <code>key(...)</code> in <code>Meta</code>, and each becomes a typed query like <code>User.query.by_email(email=...)</code>. The query surface is your access-pattern list, in code.</p>"},{"location":"concepts/single-table-design/#how-pydynantic-supports-std","title":"How pydynantic supports STD","text":"<ul> <li>Templates (Entities &amp; keys) compose PK/SK/GSI values.</li> <li><code>__entity__</code> discriminator is written on every item so reads route back to the   right typed class \u2014 the backbone of overloading and collections.</li> <li>Named access patterns (Queries) replace raw expressions.</li> <li>Collections (Collections) read multiple entity types from   one partition.</li> <li>Transactions (Transactions) keep multi-item invariants   atomic \u2014 important when one logical change touches several items.</li> </ul> <p>With those primitives, the awkward parts of STD become declarations, and you can keep thinking in entities.</p>"},{"location":"guide/batch/","title":"Batch","text":"<p>Batch operations read or write many items in as few round-trips as DynamoDB allows. <code>pydynantic</code> chunks your input to the service limits (100 per <code>BatchGetItem</code>, 25 per <code>BatchWriteItem</code>) and retries <code>UnprocessedItems</code> / <code>UnprocessedKeys</code> with backoff, so you hand it a flat list and get a clean result.</p>"},{"location":"guide/batch/#batch-get","title":"Batch get","text":"<p><code>batch_get()</code> fetches many items by key. Keys are passed positionally in primary-key order (here <code>(org_id, user_id)</code>):</p> <pre><code>users = User.batch_get([(\"acme\", \"u1\"), (\"acme\", \"u2\")])   # -&gt; list[User]\n</code></pre> <p>For a strongly consistent read or a projection:</p> <pre><code>users = User.batch_get(\n    [(\"acme\", \"u1\"), (\"acme\", \"u2\")],\n    consistent=True,\n    attributes=[\"user_id\", \"name\"],   # omitted fields fall back to model defaults\n)\n</code></pre> <p>Note</p> <p>A batch get does not guarantee order and silently omits keys that don't exist \u2014 the result may be shorter than the input. Match by id if you need to align results.</p>"},{"location":"guide/batch/#batch-write","title":"Batch write","text":"<p><code>batch_write()</code> puts and/or deletes many items in one call. <code>puts</code> takes entity instances; <code>deletes</code> takes keys:</p> <pre><code>User.batch_write(\n    puts=[\n        User(user_id=\"u4\", org_id=\"acme\", email=\"d@x.com\", name=\"Dan\"),\n        User(user_id=\"u5\", org_id=\"acme\", email=\"e@x.com\", name=\"Eve\"),\n    ],\n    deletes=[(\"acme\", \"u3\")],\n)\n</code></pre> <p>Batch writes are not transactional and not conditional</p> <p><code>BatchWriteItem</code> cannot carry conditions and does not roll back as a unit \u2014 each item is applied independently. When you need atomicity or conditional guards across items, use Transactions. Also note a batch put replaces the whole item (like <code>put()</code>), so it bypasses optimistic-locking guards.</p>"},{"location":"guide/collections/","title":"Collections","text":"<p>A collection retrieves several distinct entity types that share one partition, in a single query. This is the payoff of single-table design: one read returns an item collection, and <code>pydynantic</code> buckets it back into typed lists.</p>"},{"location":"guide/collections/#declaring-a-collection","title":"Declaring a collection","text":"<p>Subclass <code>Collection</code> and list its <code>members</code>. Every member must compose the same partition key.</p> <pre><code>from pydynantic import Collection\n\nclass OrgData(Collection):\n    members = [User, Membership, Invoice]   # all share PK = ORG#{org_id}\n</code></pre>"},{"location":"guide/collections/#querying-a-partition","title":"Querying a partition","text":"<p><code>Collection.query()</code> takes the partition-key attributes and returns a query you terminate with <code>.all()</code> or <code>.page()</code>:</p> <pre><code>result = OrgData.query(org_id=\"acme\").all()    # -&gt; CollectionResult\n\nresult.users        # list[User]\nresult.memberships  # list[Membership]\nresult.invoices     # list[Invoice]\nresult.all()        # every item across buckets, in DynamoDB return order\n</code></pre> <p>Each bucket is exposed as the entity's <code>name</code> pluralised (<code>name=\"user\"</code> \u2192 <code>result.users</code>). The result object is a <code>CollectionResult</code>.</p>"},{"location":"guide/collections/#how-discrimination-works","title":"How discrimination works","text":"<p>Every item carries the reserved <code>__entity__</code> attribute (set from the entity's <code>name=</code>). The collection query reads the whole partition, then routes each item to the member whose discriminator matches \u2014 that is how three entity types come back as three typed lists from one query.</p>"},{"location":"guide/collections/#pagination-and-projection","title":"Pagination and projection","text":"<pre><code># Cursor pagination over a collection:\npage = OrgData.query(org_id=\"acme\").limit(50).page()\npage.result.users   # list[User] in this page (page.result is a CollectionResult)\npage.has_more       # bool\nnext_page = OrgData.query(org_id=\"acme\").limit(50).page(cursor=page.cursor)\n\n# Projection \u2014 fetch only some attributes (the discriminator is always included):\nOrgData.query(org_id=\"acme\", attributes=[\"user_id\", \"name\"]).all()\n</code></pre> <p><code>.page()</code> returns a <code>CollectionPage</code>; its <code>.result</code> is a <code>CollectionResult</code> (the per-entity buckets), alongside <code>cursor</code> / <code>has_more</code>.</p> <p>Tip</p> <p>Reach for a collection when an access pattern needs several entity types from the same partition at once (e.g. \"load everything about this org\"). For a single entity type, a plain query is simpler.</p>"},{"location":"guide/crud/","title":"CRUD","text":"<p>All CRUD operations are classmethods on your entity. You never hand-write an <code>UpdateExpression</code>, and key attributes are composed from your templates automatically.</p> <p>The examples below assume the <code>User</code> entity from Entities &amp; keys.</p>"},{"location":"guide/crud/#put-create-or-replace","title":"Put (create or replace)","text":"<p><code>put()</code> writes a whole item, creating or replacing it. It returns the written object (so <code>User.put(x) is x</code>).</p> <pre><code>user = User(user_id=\"u1\", org_id=\"acme\", email=\"a@x.com\", name=\"Ana\")\nUser.put(user)\n</code></pre> <p>Pass a <code>condition</code> to guard the write \u2014 for example, a create-only put that fails if the item already exists:</p> <pre><code>from pydynantic import attr_not_exists\n\nUser.put(user, condition=attr_not_exists(\"PK\"))\n</code></pre> <p>A failed condition raises <code>ConditionCheckFailedError</code>. See Filters &amp; conditions.</p>"},{"location":"guide/crud/#get","title":"Get","text":"<p><code>get()</code> fetches by primary key and returns the typed entity, or <code>None</code> if it does not exist.</p> <pre><code>user = User.get(org_id=\"acme\", user_id=\"u1\")     # -&gt; User | None\n</code></pre> <p>Need it to raise instead of returning <code>None</code>? Use <code>get_or_raise()</code>, which raises <code>ItemNotFoundError</code>.</p> <p>For a strongly consistent read, pass <code>consistent=True</code>. To fetch only some attributes, pass a projection (omitted fields fall back to their model defaults, so include everything the entity needs to construct):</p> <pre><code>user = User.get(org_id=\"acme\", user_id=\"u1\", attributes=[\"user_id\", \"name\"])\n</code></pre>"},{"location":"guide/crud/#update-partial","title":"Update (partial)","text":"<p><code>update()</code> applies a partial change via a generated <code>UpdateExpression</code>. It returns the new state (<code>ReturnValues=\"ALL_NEW\"</code> by default).</p> <pre><code>User.update(\n    org_id=\"acme\", user_id=\"u1\",\n    set={\"name\": \"Ana B.\", \"status\": \"inactive\"},  # assign attributes\n    add={\"login_count\": 1},                         # atomic numeric/set add\n    remove=[\"nickname\"],                            # delete attributes\n)\n</code></pre> Clause Meaning <code>set</code> Assign attribute values. <code>remove</code> Delete attributes entirely. <code>add</code> Atomic numeric increment, or add elements to a set. <code>delete</code> Remove elements from a set. <p>GSI keys are kept consistent</p> <p>If <code>set</code> changes an attribute that feeds a GSI key template, <code>pydynantic</code> recomputes that index key automatically. If it can't (because a co-dependent attribute wasn't supplied), it raises <code>PydynanticError</code> telling you which attributes to also pass in <code>set=</code>.</p> <p>You can also pass a <code>condition</code>; a failed guard raises <code>ConditionCheckFailedError</code>.</p>"},{"location":"guide/crud/#delete","title":"Delete","text":"<p><code>delete()</code> removes an item by primary key.</p> <pre><code>User.delete(org_id=\"acme\", user_id=\"u1\")\n</code></pre> <p>With <code>return_values=\"ALL_OLD\"</code> it returns the deleted item (or <code>None</code> if it didn't exist); otherwise it returns <code>None</code>. A <code>condition</code> guards the delete.</p>"},{"location":"guide/crud/#next","title":"Next","text":"<ul> <li>Read many items by key pattern: Queries.</li> <li>Read/write many at once: Batch.</li> <li>Atomic multi-item changes: Transactions.</li> </ul>"},{"location":"guide/entities-and-keys/","title":"Entities &amp; keys","text":"<p>An entity is a Pydantic v2 model that carries single-table metadata: which table it lives in, a discriminator name, and the key templates that compose its DynamoDB keys.</p>"},{"location":"guide/entities-and-keys/#the-table","title":"The table","text":"<p>Create one <code>Table</code> per physical DynamoDB table. The boto3 client is injected, so credentials, region, endpoint and testing stay in your control.</p> <pre><code>import boto3\nfrom pydynantic import Table\n\ntable = Table(\n    name=\"app-prod\",\n    pk=\"PK\",                       # physical partition-key attribute name\n    sk=\"SK\",                       # physical sort-key attribute name (or None)\n    indexes={\"GSI1\": {\"pk\": \"GSI1PK\", \"sk\": \"GSI1SK\"}},\n    client=boto3.client(\"dynamodb\"),\n)\n</code></pre> <p><code>pk</code> defaults to <code>\"PK\"</code> and <code>sk</code> to <code>\"SK\"</code>. Each index maps a logical name to its physical key attribute names.</p>"},{"location":"guide/entities-and-keys/#declaring-an-entity","title":"Declaring an entity","text":"<p>Subclass <code>Entity</code>, pass <code>table=</code> and a <code>name=</code> discriminator, and declare attributes with normal Pydantic syntax. Key templates go on an inner <code>Meta</code> class via <code>key()</code>.</p> <pre><code>from datetime import datetime, timezone\nfrom pydynantic import Entity, Field, key\n\nclass User(Entity, table=table, name=\"user\"):\n    user_id: str\n    org_id: str\n    email: str\n    name: str\n    status: str = \"active\"\n    login_count: int = 0\n    created_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))\n\n    class Meta:\n        primary = key(pk=\"ORG#{org_id}\", sk=\"USER#{user_id}\")\n        by_email = key(index=\"GSI1\", pk=\"EMAIL#{email}\", sk=\"USER#{user_id}\")\n</code></pre> <p>The <code>name=\"user\"</code> discriminator is written to a reserved <code>__entity__</code> attribute on every item, which is how reads (and collections) route an item back to the right typed class.</p>"},{"location":"guide/entities-and-keys/#key-templates","title":"Key templates","text":"<p>A template is a string with <code>{placeholder}</code> fields that reference entity attributes. Placeholders are resolved against the attribute values when an item is written or a key is built:</p> Template Attributes Rendered key <code>\"ORG#{org_id}\"</code> <code>org_id=\"acme\"</code> <code>ORG#acme</code> <code>\"USER#{user_id}\"</code> <code>user_id=\"u1\"</code> <code>USER#u1</code> <code>\"EMAIL#{email}\"</code> <code>email=\"a@x.com\"</code> <code>EMAIL#a@x.com</code> <p>Templates support literal prefixes/suffixes and multiple placeholders in a single key (e.g. <code>\"TENANT#{tenant}#USER#{user_id}\"</code>). Values are rendered deterministically: <code>datetime</code>/<code>date</code> use ISO-8601, <code>Enum</code> uses its value, <code>bool</code> is lowercased.</p> <p>Warning</p> <p>If a template references an attribute you don't supply when building a key, <code>pydynantic</code> raises <code>KeyTemplateError</code>. Pass every attribute the relevant template needs.</p>"},{"location":"guide/entities-and-keys/#the-primary-key-and-gsis","title":"The <code>primary</code> key and GSIs","text":"<ul> <li>The <code>Meta</code> attribute named <code>primary</code> is the entity's primary key (no <code>index=</code>).</li> <li>Any other <code>key(..., index=\"GSI1\", ...)</code> declares a named access pattern over that GSI.</li> </ul> <p>Each named key becomes a typed query \u2014 <code>User.query.primary(...)</code>, <code>User.query.by_email(...)</code> \u2014 covered in Queries. When an <code>update()</code> changes an attribute that feeds a GSI key, <code>pydynantic</code> recomputes that index key automatically (see CRUD).</p>"},{"location":"guide/entities-and-keys/#special-attribute-markers","title":"Special attribute markers","text":"<p><code>pydynantic</code> ships field markers that opt an attribute into managed behaviour. Each is a drop-in replacement for a Pydantic default:</p> <pre><code>from pydynantic import version_attr, ttl_attr, created_at_attr, updated_at_attr\n\nclass Order(Entity, table=table, name=\"order\"):\n    order_id: str\n    customer_id: str\n    version: int = version_attr()         # optimistic locking\n    created_at: datetime | None = created_at_attr()\n    updated_at: datetime | None = updated_at_attr()\n    expires_at: datetime | None = ttl_attr()\n\n    class Meta:\n        primary = key(pk=\"CUSTOMER#{customer_id}\", sk=\"ORDER#{order_id}\")\n</code></pre> <ul> <li><code>version_attr()</code> \u2014 see Optimistic locking.</li> <li><code>ttl_attr()</code>, <code>created_at_attr()</code>,   <code>updated_at_attr()</code> \u2014 see TTL &amp; timestamps.</li> </ul>"},{"location":"guide/filters-and-conditions/","title":"Filters &amp; conditions","text":"<p><code>pydynantic</code> builds DynamoDB expressions from a small fluent DSL, so you never juggle <code>ExpressionAttributeNames</code> / <code>ExpressionAttributeValues</code> or risk placeholder collisions. The same <code>Condition</code> objects power both filters (narrow query results) and conditions (guard writes).</p>"},{"location":"guide/filters-and-conditions/#building-conditions-with-f","title":"Building conditions with <code>F</code>","text":"<p><code>F</code> references an attribute by name; comparing it produces a <code>Condition</code>:</p> <pre><code>from pydynantic import F\n\nF(\"status\") == \"active\"\nF(\"login_count\") &gt; 0\nF(\"name\") != \"Ana\"\n</code></pre> <p>Operators and methods:</p> Expression Meaning <code>F(\"a\") == v</code>, <code>!= v</code>, <code>&lt; v</code>, <code>&lt;= v</code>, <code>&gt; v</code>, <code>&gt;= v</code> comparisons <code>F(\"a\").between(lo, hi)</code> inclusive range <code>F(\"a\").begins_with(prefix)</code> string prefix <code>F(\"a\").contains(v)</code> substring / set membership <code>F(\"a\").is_in([v1, v2])</code> one of a list <code>F(\"a\").exists()</code> / <code>F(\"a\").not_exists()</code> attribute presence"},{"location":"guide/filters-and-conditions/#combining-conditions","title":"Combining conditions","text":"<p>Combine with <code>&amp;</code> (AND), <code>|</code> (OR) and <code>~</code> (NOT). Parenthesize to make precedence explicit:</p> <pre><code>(F(\"status\") == \"active\") &amp; (F(\"login_count\") &gt; 0)\n(F(\"status\") == \"active\") | (F(\"status\") == \"trial\")\n~(F(\"status\") == \"banned\")\n</code></pre>"},{"location":"guide/filters-and-conditions/#filters-on-queries-and-scans","title":"Filters on queries and scans","text":"<p><code>.filter(...)</code> applies a <code>FilterExpression</code> after the key condition. It narrows the returned items but is billed on items read, so it complements \u2014 never replaces \u2014 a good key design.</p> <pre><code>(User.query.primary(org_id=\"acme\")\n     .filter((F(\"status\") == \"active\") &amp; (F(\"login_count\") &gt; 0))\n     .all())\n\nUser.scan().filter(F(\"status\") == \"active\").count()\n</code></pre>"},{"location":"guide/filters-and-conditions/#conditions-on-writes","title":"Conditions on writes","text":"<p>Pass a <code>condition=</code> to <code>put()</code>, <code>update()</code> or <code>delete()</code> to make the write conditional. A failed guard raises <code>ConditionCheckFailedError</code>.</p> <pre><code># Create-only put \u2014 fails if the item already exists.\nfrom pydynantic import attr_not_exists\nUser.put(user, condition=attr_not_exists(\"PK\"))\n\n# Conditional update \u2014 only deactivate an account that is currently active.\nUser.update(\n    org_id=\"acme\", user_id=\"u1\",\n    set={\"status\": \"inactive\"},\n    condition=F(\"status\") == \"active\",\n)\n</code></pre>"},{"location":"guide/filters-and-conditions/#attr_exists-attr_not_exists","title":"<code>attr_exists</code> / <code>attr_not_exists</code>","text":"<p><code>attr_exists</code> and <code>attr_not_exists</code> are convenience helpers that build a presence condition on a raw attribute path \u2014 handy for create-only or update-only guards keyed on the physical <code>PK</code>:</p> <pre><code>from pydynantic import attr_exists, attr_not_exists\n\nUser.put(user, condition=attr_not_exists(\"PK\"))         # must NOT already exist\nUser.delete(org_id=\"acme\", user_id=\"u1\",\n            condition=attr_exists(\"PK\"))                # must already exist\n</code></pre> <p>Note</p> <p><code>ExpressionAttributeNames</code> and <code>ExpressionAttributeValues</code> are generated for you with collision-free <code>#name</code> / <code>:value</code> placeholders \u2014 even for dotted attribute paths and repeated attributes.</p>"},{"location":"guide/marshalling/","title":"Marshalling","text":"<p>DynamoDB only understands its own attribute-value types. <code>pydynantic</code> marshals your Pydantic fields to and from those types transparently, so you model with native Python types and never touch a raw <code>{\"S\": ...}</code> / <code>{\"N\": ...}</code> map.</p>"},{"location":"guide/marshalling/#supported-types","title":"Supported types","text":"Python type DynamoDB representation <code>str</code> <code>S</code> <code>int</code>, <code>float</code>, <code>Decimal</code> <code>N</code> (stored as <code>Decimal</code>) <code>bool</code> <code>BOOL</code> <code>bytes</code> <code>B</code> <code>datetime</code>, <code>date</code> <code>S</code> (ISO-8601 string) <code>UUID</code> <code>S</code> <code>Enum</code> underlying value's type <code>list</code> <code>L</code> <code>dict</code> <code>M</code> <code>set</code> (of str/number/bytes) <code>SS</code> / <code>NS</code> / <code>BS</code> nested Pydantic model <code>M</code> <pre><code>from datetime import datetime, timezone\nfrom decimal import Decimal\nfrom enum import Enum\nfrom uuid import UUID\nfrom pydantic import BaseModel\nfrom pydynantic import Entity, key\n\nclass Tier(str, Enum):\n    free = \"free\"\n    pro = \"pro\"\n\nclass Address(BaseModel):\n    city: str\n    zip: str\n\nclass Account(Entity, table=table, name=\"account\"):\n    account_id: UUID\n    org_id: str\n    balance: Decimal = Decimal(\"0\")\n    tier: Tier = Tier.free\n    tags: set[str] = set()\n    address: Address | None = None\n    created_at: datetime = datetime(2024, 1, 1, tzinfo=timezone.utc)\n\n    class Meta:\n        primary = key(pk=\"ORG#{org_id}\", sk=\"ACCOUNT#{account_id}\")\n</code></pre>"},{"location":"guide/marshalling/#important-behaviours","title":"Important behaviours","text":"<ul> <li>Numbers are stored as <code>Decimal</code>. Floats are converted to <code>Decimal</code> on write to   avoid floating-point drift. Read back, a field typed <code>float</code> is coerced by Pydantic;   prefer <code>Decimal</code> directly for money.</li> <li><code>datetime</code> / <code>date</code> are ISO-8601 strings. Because they sort lexicographically, they   work naturally in sort keys and <code>between</code> ranges.</li> <li><code>None</code> values are omitted. DynamoDB can't store a true null in a typed item, so   <code>None</code> fields are dropped \u2014 and re-materialise from the field's default on read.</li> <li>Empty sets are omitted. DynamoDB rejects empty <code>SS</code>/<code>NS</code>/<code>BS</code>, so an empty set is   dropped and comes back as the field default.</li> </ul> <p>Projections and defaults</p> <p>A projected read (<code>get(..., attributes=...)</code>, <code>.attributes([...])</code>) returns only the requested attributes; every omitted field falls back to its model default. Include everything a required field needs, or validation will fail when the entity is reconstructed.</p>"},{"location":"guide/marshalling/#round-trip-helpers","title":"Round-trip helpers","text":"<p>The marshalling is exercised by <code>Entity.to_dynamo()</code> (model \u2192 DynamoDB item) and <code>Entity.from_dynamo(item)</code> (item \u2192 model). You rarely call these directly \u2014 every CRUD, query, batch and transaction path uses them \u2014 but they're useful when integrating with raw boto3 calls.</p>"},{"location":"guide/optimistic-locking/","title":"Optimistic locking","text":"<p>Optimistic locking prevents lost updates without holding a lock: each write checks that the item hasn't changed since you read it, and bumps a version number. A lost race fails loudly instead of silently overwriting.</p>"},{"location":"guide/optimistic-locking/#marking-the-version-field","title":"Marking the version field","text":"<p>Declare an integer field with <code>version_attr()</code>:</p> <pre><code>from pydynantic import Entity, key, version_attr\n\nclass Order(Entity, table=table, name=\"order\"):\n    order_id: str\n    customer_id: str\n    total: float = 0.0\n    version: int = version_attr()        # starts at 0\n\n    class Meta:\n        primary = key(pk=\"CUSTOMER#{customer_id}\", sk=\"ORDER#{order_id}\")\n</code></pre>"},{"location":"guide/optimistic-locking/#how-it-works","title":"How it works","text":"<p>Once a version field is present, every write increments <code>version</code>. Whether the write is also guarded (raises instead of overwriting a concurrent change) depends on the call:</p> <ul> <li><code>put</code> guards automatically. It carries the full item \u2014 including the <code>version</code> you   read \u2014 so it guards on the previous value: \"the stored version must still be what I   last saw\" (a first create is allowed because the item doesn't yet exist).</li> <li><code>update</code> is a blind, keyed partial write with no item in hand, so it cannot know the   expected version. Pass <code>expected_version=</code> to opt into the guard (see below). Without it,   <code>update</code> still increments <code>version</code> but does not guard \u2014 concurrent updates can lose   each other's writes.</li> </ul> <p>When a guard fails (someone else wrote in between), the write raises <code>OptimisticLockError</code> (a subclass of <code>ConditionCheckFailedError</code>).</p> <pre><code>order = Order(order_id=\"o1\", customer_id=\"acme\", total=120.0)\nOrder.put(order)          # version 0 -&gt; 1; the returned object carries version 1\n\norder.total = 130.0\nOrder.put(order)          # guards on version 1; succeeds, now version 2\n</code></pre> <p>On a failed <code>put</code>, the in-memory version bump is rolled back so the object still reflects the stored version and the call can be retried after re-reading.</p>"},{"location":"guide/optimistic-locking/#guarding-an-update","title":"Guarding an <code>update</code>","text":"<p>Read the item, then pass the version you saw as <code>expected_version=</code>. If the stored version moved in between, the write raises instead of clobbering it:</p> <pre><code>from pydynantic import OptimisticLockError\n\norder = Order.get_or_raise(customer_id=\"acme\", order_id=\"o1\")\ntry:\n    Order.update(\n        customer_id=\"acme\",\n        order_id=\"o1\",\n        set={\"total\": 99.0},\n        expected_version=order.version,   # guard on what we read\n    )\nexcept OptimisticLockError:\n    fresh = Order.get_or_raise(customer_id=\"acme\", order_id=\"o1\")\n    # re-apply the change against the fresh version, then retry\n</code></pre> <p>Passing <code>expected_version=</code> to an entity without a <code>version_attr()</code> field raises <code>ValueError</code> \u2014 there is nothing to guard on.</p> <p>Warning</p> <p>An <code>update</code> without <code>expected_version=</code> increments <code>version</code> but does not guard, so it can silently lose a concurrent write. A batch write replaces items unconditionally and bypasses the guard entirely \u2014 use transactions when you need conditional multi-item writes.</p>"},{"location":"guide/pagination/","title":"Pagination","text":"<p>For large result sets, fetch one page at a time and resume with an opaque cursor. <code>.page()</code> returns a <code>Page</code>; its <code>cursor</code> is safe to hand to an HTTP client or store in JSON.</p>"},{"location":"guide/pagination/#paging-through-a-query","title":"Paging through a query","text":"<pre><code>page = User.query.primary(org_id=\"acme\").limit(25).page()\n\npage.items        # list[User]\npage.cursor       # opaque token, or None when the result set is exhausted\npage.has_more     # True while a cursor was returned\n\nnext_page = User.query.primary(org_id=\"acme\").limit(25).page(cursor=page.cursor)\n</code></pre> <p><code>.page()</code> is available on queries, scans, and collections.</p>"},{"location":"guide/pagination/#the-cursor-is-opaque","title":"The cursor is opaque","text":"<p>A cursor is a versioned, base64url token without <code>=</code> padding, so it embeds directly in URLs, query strings and JSON bodies. Treat it as a black box:</p> <ul> <li>Pass back verbatim whatever <code>page.cursor</code> produced.</li> <li>Never hand-build, mutate, or parse a cursor.</li> </ul> <p>A stale, malformed, or wrong-version cursor fails loudly \u2014 <code>decode_cursor</code> raises <code>PydynanticError</code> rather than silently misbehaving.</p> <p>Not signed</p> <p>Cursors are not HMAC-signed. They encode only a DynamoDB key the caller could already query directly, so there is no secret to protect; versioning plus validation buys well-defined failure without key management.</p>"},{"location":"guide/pagination/#looping-over-all-pages","title":"Looping over all pages","text":"<p>For an HTTP API you typically return one page per request (see the recipes). To drain everything in a worker, either use <code>.all()</code> / <code>.iter()</code> (which paginate internally) or loop on the cursor:</p> <pre><code>cursor = None\nwhile True:\n    page = User.query.primary(org_id=\"acme\").limit(100).page(cursor=cursor)\n    for user in page.items:\n        process(user)\n    if not page.has_more:\n        break\n    cursor = page.cursor\n</code></pre>"},{"location":"guide/pagination/#low-level-helpers","title":"Low-level helpers","text":"<p><code>encode_cursor</code> and <code>decode_cursor</code> convert between a DynamoDB <code>LastEvaluatedKey</code> and the opaque token. You rarely call these directly \u2014 <code>.page()</code> does it for you \u2014 but they are exported for advanced integrations.</p>"},{"location":"guide/queries/","title":"Queries","text":"<p>A query reads a slice of one partition by key condition. In <code>pydynantic</code>, every named key in <code>Meta</code> becomes a typed access pattern on <code>Entity.query</code>, so you call the pattern by name instead of writing a <code>KeyConditionExpression</code>.</p> <pre><code>class User(Entity, table=table, name=\"user\"):\n    ...\n    class Meta:\n        primary = key(pk=\"ORG#{org_id}\", sk=\"USER#{user_id}\")\n        by_email = key(index=\"GSI1\", pk=\"EMAIL#{email}\", sk=\"USER#{user_id}\")\n</code></pre>"},{"location":"guide/queries/#querying-a-partition","title":"Querying a partition","text":"<p>Start from the access pattern, supplying the attributes its partition key needs:</p> <pre><code># All users in an org (partition + begins_with on the sort key):\nusers = (User.query.primary(org_id=\"acme\")\n                   .begins_with(\"USER#\")\n                   .limit(50)\n                   .all())                      # -&gt; list[User]\n</code></pre> <p>Query a GSI exactly the same way:</p> <pre><code>user = User.query.by_email(email=\"a@x.com\").one_or_none()   # -&gt; User | None\n</code></pre>"},{"location":"guide/queries/#sort-key-conditions","title":"Sort-key conditions","text":"<p>After choosing a pattern, narrow the sort key with one of:</p> Method Meaning <code>.eq(v)</code> exact match <code>.begins_with(prefix)</code> prefix match <code>.between(low, high)</code> inclusive range <code>.gt(v)</code> / <code>.gte(v)</code> greater (or equal) <code>.lt(v)</code> / <code>.lte(v)</code> less (or equal) <p>Because sort keys sort lexicographically, ISO-8601 timestamp prefixes give you natural date ranges:</p> <pre><code>User.query.primary(org_id=\"acme\").between(\"USER#a\", \"USER#m\").all()\n</code></pre>"},{"location":"guide/queries/#ordering-limits-projection-consistency","title":"Ordering, limits, projection, consistency","text":"<pre><code>(User.query.primary(org_id=\"acme\")\n     .descending()                  # newest-first; .ascending() is the default\n     .limit(25)                     # cap the page size\n     .attributes([\"user_id\", \"name\"])  # projection; omitted fields use defaults\n     .consistent()                  # strongly consistent read (not valid on a GSI)\n     .all())\n</code></pre>"},{"location":"guide/queries/#terminals","title":"Terminals","text":"<p>A builder does nothing until you call a terminal:</p> Terminal Returns <code>.all()</code> <code>list[E]</code> \u2014 every matching item (paginates internally) <code>.iter()</code> <code>Iterator[E]</code> \u2014 lazy, streams pages <code>.first()</code> <code>E \\| None</code> \u2014 the first item, or <code>None</code> <code>.one()</code> <code>E</code> \u2014 exactly one; raises otherwise <code>.one_or_none()</code> <code>E \\| None</code> \u2014 at most one; raises if more <code>.page(cursor=...)</code> a <code>Page</code> for cursor pagination <code>.count()</code> <code>int</code> \u2014 server-side count, no items materialised <pre><code># Exactly one (raises ItemNotFoundError / MultipleResultsError otherwise):\nuser = User.query.by_email(email=\"a@x.com\").one()\n\n# Server-side count with a filter, nothing materialised:\nactive = User.query.primary(org_id=\"acme\").filter(F(\"status\") == \"active\").count()\n</code></pre> <p><code>.one()</code> raises <code>ItemNotFoundError</code> when empty and <code>MultipleResultsError</code> when more than one matches.</p>"},{"location":"guide/queries/#filters","title":"Filters","text":"<p><code>.filter(...)</code> applies a server-side <code>FilterExpression</code> after the key condition (it narrows results but is billed on items read). See Filters &amp; conditions.</p>"},{"location":"guide/queries/#scan","title":"Scan","text":"<p>When no key condition applies, <code>scan()</code> reads the whole table. It automatically filters on the <code>__entity__</code> discriminator so only this entity's items come back, and exposes the same builder surface (<code>ScanBuilder</code>): <code>.filter</code>, <code>.limit</code>, <code>.attributes</code>, <code>.all</code>, <code>.first</code>, <code>.iter</code>, <code>.page</code>, <code>.count</code>.</p> <pre><code>all_users = User.scan().all()\nUser.scan().filter(F(\"status\") == \"active\").count()\n</code></pre> <p>Tip</p> <p>Prefer <code>query</code> whenever a key condition applies \u2014 a scan reads every item in the table and is far more expensive than a partition query.</p>"},{"location":"guide/transactions/","title":"Transactions","text":"<p>A transaction applies up to 100 writes as a single, atomic <code>TransactWriteItems</code> call: either every action succeeds or none do. Use it to keep multi-item invariants consistent.</p>"},{"location":"guide/transactions/#the-context-manager","title":"The context manager","text":"<p><code>transaction()</code> yields a <code>Transaction</code> to queue actions. It commits on a clean exit; if the <code>with</code> block raises, <code>commit</code> is never reached, so nothing is sent (rollback).</p> <pre><code>from pydynantic import transaction, F\n\nwith transaction(table) as tx:\n    tx.put(Order(order_id=\"o1\", customer_id=\"acme\", total=120.0))\n    tx.update(User, key=(\"acme\", \"u1\"), add={\"orders\": 1})\n    tx.condition_check(Account, key=(\"acme\",), condition=F(\"balance\") &gt;= 100)\n# Commits here. A failed condition cancels everything atomically.\n</code></pre>"},{"location":"guide/transactions/#actions","title":"Actions","text":"Method Purpose <code>tx.put(item, condition=...)</code> queue a <code>Put</code> <code>tx.update(Entity, key=..., set=..., add=..., remove=..., delete=..., condition=...)</code> queue an <code>Update</code> <code>tx.delete(Entity, key=..., condition=...)</code> queue a <code>Delete</code> <code>tx.condition_check(Entity, key=..., condition=...)</code> guard with no write <p><code>key=</code> accepts the primary-key attributes (a tuple in primary-key order, e.g. <code>(\"acme\", \"u1\")</code>). The <code>set</code> / <code>add</code> / <code>remove</code> / <code>delete</code> clauses mirror <code>update()</code>.</p>"},{"location":"guide/transactions/#atomicity-and-failures","title":"Atomicity and failures","text":"<p>If any queued condition fails at commit time, DynamoDB cancels the whole transaction and <code>pydynantic</code> raises <code>TransactionCanceledError</code>. Its <code>.reasons</code> lists a cancellation reason per action, in order, so you can tell which guard failed.</p> <pre><code>from pydynantic import transaction, TransactionCanceledError, F\n\ntry:\n    with transaction(table) as tx:\n        tx.update(Account, key=(\"acme\",), add={\"balance\": -100},\n                  condition=F(\"balance\") &gt;= 100)\n        tx.put(Order(order_id=\"o1\", customer_id=\"acme\", total=100.0))\nexcept TransactionCanceledError as exc:\n    print(exc.reasons)   # per-action reasons; e.g. ConditionalCheckFailed on the guard\n</code></pre> <p><code>condition_check</code> is a read-only guard</p> <p>Use <code>tx.condition_check(...)</code> to assert a fact about an item you are not writing \u2014 for example \"the account still exists and is active\" \u2014 so the transaction fails if the assumption no longer holds.</p>"},{"location":"guide/ttl-and-timestamps/","title":"TTL &amp; timestamps","text":"<p><code>pydynantic</code> ships field markers that manage common metadata for you: a DynamoDB TTL attribute and auto-stamped created/updated timestamps. Each is a drop-in replacement for a Pydantic field default.</p> <pre><code>from datetime import datetime\nfrom pydynantic import Entity, key, ttl_attr, created_at_attr, updated_at_attr\n\nclass Session(Entity, table=table, name=\"session\"):\n    session_id: str\n    user_id: str\n    created_at: datetime | None = created_at_attr()\n    updated_at: datetime | None = updated_at_attr()\n    expires_at: datetime | None = ttl_attr()\n\n    class Meta:\n        primary = key(pk=\"USER#{user_id}\", sk=\"SESSION#{session_id}\")\n</code></pre>"},{"location":"guide/ttl-and-timestamps/#created_at_attr","title":"<code>created_at_attr()</code>","text":"<p><code>created_at_attr()</code> marks a <code>datetime | None</code> field as the creation timestamp. On the first <code>put</code> (while the field is still <code>None</code>) it is stamped with <code>datetime.now(timezone.utc)</code>. Later puts leave it untouched, and <code>update</code> never modifies it.</p>"},{"location":"guide/ttl-and-timestamps/#updated_at_attr","title":"<code>updated_at_attr()</code>","text":"<p><code>updated_at_attr()</code> marks a <code>datetime | None</code> field as the modification timestamp. It is stamped on every <code>put</code>, and on every <code>update</code> \u2014 unless the caller explicitly supplies it in <code>set=</code>, in which case the caller's value wins.</p>"},{"location":"guide/ttl-and-timestamps/#ttl_attr","title":"<code>ttl_attr()</code>","text":"<p><code>ttl_attr()</code> marks the DynamoDB Time-To-Live attribute. DynamoDB requires the TTL attribute to be a Number holding a Unix epoch timestamp in seconds. You usually declare the field as <code>datetime | None</code> (the common case) or <code>int</code>:</p> <ul> <li>On every <code>put</code>, the stored value is written as epoch-seconds, regardless of the model's   normal ISO datetime encoding.</li> <li>Reading back coerces the epoch into a UTC <code>datetime</code>, so sub-second precision is dropped.</li> <li>The default is <code>None</code> (TTL is optional); when <code>None</code>, the attribute is omitted entirely.</li> </ul> <pre><code>from datetime import datetime, timedelta, timezone\n\nsession = Session(\n    session_id=\"s1\",\n    user_id=\"u1\",\n    expires_at=datetime.now(timezone.utc) + timedelta(hours=1),\n)\nSession.put(session)   # expires_at stored as epoch-seconds; DynamoDB expires it later\n</code></pre> <p>Use timezone-aware datetimes for TTL</p> <p>TTL <code>datetime</code> values should be timezone-aware. A naive datetime is passed to <code>.timestamp()</code> unchanged (Python interprets it in the local timezone); <code>pydynantic</code> does not reinterpret it, so prefer tz-aware values to avoid surprises.</p> <p>Enable TTL on the table</p> <p>These markers control how <code>pydynantic</code> writes the attribute. Actual expiry requires TimeToLive to be enabled on the table itself (via the console, CDK, or Terraform) \u2014 <code>pydynantic</code> does not provision infrastructure. DynamoDB also deletes expired items lazily (typically within 48 hours), so don't rely on TTL for precise timing; pair it with a filter on <code>expires_at</code> if you need exactness (see the soft-delete recipe).</p>"},{"location":"migrating/from-dynamodb-toolbox/","title":"Migrating from DynamoDB-Toolbox","text":"<p>DynamoDB-Toolbox is a popular JavaScript single-table toolkit built around a <code>Table</code> plus <code>Entity</code> definitions and command objects. <code>pydynantic</code> offers the same model in Python on Pydantic v2.</p>"},{"location":"migrating/from-dynamodb-toolbox/#concept-mapping","title":"Concept mapping","text":"DynamoDB-Toolbox pydynantic <code>new Table({ name, partitionKey, sortKey, indexes })</code> <code>Table</code> <code>new Entity({ schema, ... })</code> subclass <code>Entity</code> <code>schema({ attr: string() })</code> Pydantic fields <code>.key()</code> computed partition/sort <code>key(pk=\"...\", sk=\"...\")</code> templates <code>PutItemCommand</code> / <code>entity.build(PutItem).send()</code> <code>Entity.put(...)</code> <code>GetItemCommand</code> <code>Entity.get(...)</code> <code>UpdateItemCommand</code> (<code>$add</code>, <code>$remove</code>) <code>Entity.update(add=..., remove=...)</code> <code>QueryCommand</code> (<code>{ index, ... }</code>) named <code>Entity.query.&lt;pattern&gt;(...)</code> <code>condition: { attr, eq }</code> <code>condition=F(\"attr\") == ...</code> <code>BatchGetCommand</code> / <code>BatchWriteCommand</code> <code>batch_get</code> / <code>batch_write</code> <code>executeTransactWrite([...])</code> <code>transaction(table)</code>"},{"location":"migrating/from-dynamodb-toolbox/#table-entity","title":"Table &amp; entity","text":"DynamoDB-Toolbox (JS)pydynantic (Python) <pre><code>import { Table, Entity, schema, string, number } from \"dynamodb-toolbox\";\n\nconst table = new Table({\n  name: \"app-prod\",\n  partitionKey: { name: \"PK\", type: \"string\" },\n  sortKey: { name: \"SK\", type: \"string\" },\n  indexes: { GSI1: { type: \"global\", partitionKey: { name: \"GSI1PK\", type: \"string\" },\n                     sortKey: { name: \"GSI1SK\", type: \"string\" } } },\n  documentClient,\n});\n\nconst User = new Entity({\n  name: \"user\",\n  table,\n  schema: schema({\n    userId: string().key(),\n    orgId:  string().key(),\n    email:  string(),\n    name:   string(),\n    status: string().default(\"active\"),\n    loginCount: number().default(0),\n  }),\n  computeKey: ({ orgId, userId }) =&gt; ({ PK: `ORG#${orgId}`, SK: `USER#${userId}` }),\n});\n</code></pre> <pre><code>import boto3\nfrom pydynantic import Table, Entity, key\n\ntable = Table(\n    name=\"app-prod\",\n    pk=\"PK\", sk=\"SK\",\n    indexes={\"GSI1\": {\"pk\": \"GSI1PK\", \"sk\": \"GSI1SK\"}},\n    client=boto3.client(\"dynamodb\"),\n)\n\nclass User(Entity, table=table, name=\"user\"):\n    user_id: str\n    org_id: str\n    email: str\n    name: str\n    status: str = \"active\"\n    login_count: int = 0\n\n    class Meta:\n        primary = key(pk=\"ORG#{org_id}\", sk=\"USER#{user_id}\")\n        by_email = key(index=\"GSI1\", pk=\"EMAIL#{email}\", sk=\"USER#{user_id}\")\n</code></pre>"},{"location":"migrating/from-dynamodb-toolbox/#crud","title":"CRUD","text":"DynamoDB-Toolbox (JS)pydynantic (Python) <pre><code>await User.build(PutItemCommand)\n  .item({ userId: \"u1\", orgId: \"acme\", email: \"a@x.com\", name: \"Ana\" })\n  .send();\n\nconst { Item } = await User.build(GetItemCommand)\n  .key({ orgId: \"acme\", userId: \"u1\" }).send();\n\nawait User.build(UpdateItemCommand)\n  .item({ orgId: \"acme\", userId: \"u1\", name: \"Ana B.\", loginCount: $add(1) })\n  .send();\n\nawait User.build(DeleteItemCommand)\n  .key({ orgId: \"acme\", userId: \"u1\" }).send();\n</code></pre> <pre><code>User.put(User(user_id=\"u1\", org_id=\"acme\", email=\"a@x.com\", name=\"Ana\"))\n\nuser = User.get(org_id=\"acme\", user_id=\"u1\")\n\nUser.update(\n    org_id=\"acme\", user_id=\"u1\",\n    set={\"name\": \"Ana B.\"},\n    add={\"login_count\": 1},\n)\n\nUser.delete(org_id=\"acme\", user_id=\"u1\")\n</code></pre>"},{"location":"migrating/from-dynamodb-toolbox/#queries-transactions","title":"Queries &amp; transactions","text":"DynamoDB-Toolbox (JS)pydynantic (Python) <pre><code>const { Items } = await table.build(QueryCommand)\n  .query({ partition: \"ORG#acme\", range: { beginsWith: \"USER#\" } })\n  .entities(User)\n  .send();\n\nawait table.build(TransactWriteCommand)\n  .commands([\n    User.build(UpdateTransaction).item({ orgId: \"acme\", userId: \"u1\", orders: $add(1) }),\n    Order.build(PutTransaction).item({ ... }),\n  ])\n  .send();\n</code></pre> <pre><code>users = User.query.primary(org_id=\"acme\").begins_with(\"USER#\").all()\n\nfrom pydynantic import transaction\nwith transaction(table) as tx:\n    tx.update(User, key=(\"acme\", \"u1\"), add={\"orders\": 1})\n    tx.put(Order(order_id=\"o1\", customer_id=\"acme\", total=10.0))\n</code></pre>"},{"location":"migrating/from-dynamodb-toolbox/#whats-different","title":"What's different","text":"<ul> <li>No command builders. Where DynamoDB-Toolbox builds a <code>*Command</code> and calls <code>.send()</code>,   <code>pydynantic</code> exposes plain classmethods (<code>put</code>, <code>get</code>, <code>update</code>, <code>delete</code>) and a query   builder whose terminals run immediately.</li> <li>Synchronous. No promises/<code>await</code>; results return directly.</li> <li>Pydantic schema. Field types, defaults and nested models are standard Pydantic v2.</li> <li>Named access patterns. Instead of passing <code>{ index }</code> to a <code>QueryCommand</code>, each key   in <code>Meta</code> becomes a typed method on <code>Entity.query</code> (see Queries).</li> <li>Cross-entity reads use a <code>Collection</code>   (Collections) rather than <code>.entities(A, B)</code> on a query.</li> </ul>"},{"location":"migrating/from-electrodb/","title":"Migrating from ElectroDB","text":"<p>ElectroDB is the JavaScript/TypeScript single-table library many teams reach for. <code>pydynantic</code> covers the same ground in Python on Pydantic v2. The concepts map closely; the syntax is Pythonic.</p>"},{"location":"migrating/from-electrodb/#concept-mapping","title":"Concept mapping","text":"ElectroDB pydynantic <code>new Entity({...})</code> subclass <code>Entity</code> <code>new Service({...})</code> a <code>Collection</code> (multi-entity partition reads) <code>attributes: { ... }</code> Pydantic fields on the class <code>indexes: { primary: {...} }</code> <code>key(...)</code> in inner <code>Meta</code> composite <code>pk</code>/<code>sk</code> <code>template</code> <code>key(pk=\"...\", sk=\"...\")</code> templates secondary <code>index: \"gsi1pk-...\"</code> <code>key(index=\"GSI1\", ...)</code> <code>entity.get({...}).go()</code> <code>Entity.get(...)</code> <code>entity.put({...}).go()</code> <code>Entity.put(...)</code> <code>entity.update({...}).set({...}).go()</code> <code>Entity.update(set=...)</code> <code>entity.query.byX({...}).go()</code> <code>Entity.query.by_x(...)</code> <code>.where(({attr}, {eq}) =&gt; ...)</code> <code>.filter(F(\"attr\") == ...)</code> <code>{ cursor }</code> on <code>.go()</code> <code>.page(cursor=...)</code> <code>add</code>/<code>subtract</code> in update <code>add={...}</code> (negative delta to subtract)"},{"location":"migrating/from-electrodb/#defining-an-entity","title":"Defining an entity","text":"ElectroDB (JS)pydynantic (Python) <pre><code>import { Entity } from \"electrodb\";\n\nconst User = new Entity({\n  model: { entity: \"user\", version: \"1\", service: \"app\" },\n  attributes: {\n    userId: { type: \"string\" },\n    orgId:  { type: \"string\" },\n    email:  { type: \"string\" },\n    name:   { type: \"string\" },\n    status: { type: \"string\", default: \"active\" },\n  },\n  indexes: {\n    primary: {\n      pk: { field: \"PK\", composite: [\"orgId\"], template: \"ORG#${orgId}\" },\n      sk: { field: \"SK\", composite: [\"userId\"], template: \"USER#${userId}\" },\n    },\n    byEmail: {\n      index: \"GSI1\",\n      pk: { field: \"GSI1PK\", composite: [\"email\"], template: \"EMAIL#${email}\" },\n      sk: { field: \"GSI1SK\", composite: [\"userId\"], template: \"USER#${userId}\" },\n    },\n  },\n}, { client, table: \"app-prod\" });\n</code></pre> <pre><code>from pydynantic import Entity, key\n\nclass User(Entity, table=table, name=\"user\"):\n    user_id: str\n    org_id: str\n    email: str\n    name: str\n    status: str = \"active\"\n\n    class Meta:\n        primary = key(pk=\"ORG#{org_id}\", sk=\"USER#{user_id}\")\n        by_email = key(index=\"GSI1\", pk=\"EMAIL#{email}\", sk=\"USER#{user_id}\")\n</code></pre>"},{"location":"migrating/from-electrodb/#crud","title":"CRUD","text":"ElectroDB (JS)pydynantic (Python) <pre><code>await User.put({ userId: \"u1\", orgId: \"acme\", email: \"a@x.com\", name: \"Ana\" }).go();\n\nconst { data } = await User.get({ orgId: \"acme\", userId: \"u1\" }).go();\n\nawait User.update({ orgId: \"acme\", userId: \"u1\" })\n  .set({ name: \"Ana B.\", status: \"inactive\" })\n  .add({ loginCount: 1 })\n  .go();\n\nawait User.delete({ orgId: \"acme\", userId: \"u1\" }).go();\n</code></pre> <pre><code>User.put(User(user_id=\"u1\", org_id=\"acme\", email=\"a@x.com\", name=\"Ana\"))\n\nuser = User.get(org_id=\"acme\", user_id=\"u1\")     # -&gt; User | None\n\nUser.update(\n    org_id=\"acme\", user_id=\"u1\",\n    set={\"name\": \"Ana B.\", \"status\": \"inactive\"},\n    add={\"login_count\": 1},\n)\n\nUser.delete(org_id=\"acme\", user_id=\"u1\")\n</code></pre>"},{"location":"migrating/from-electrodb/#queries-filters","title":"Queries &amp; filters","text":"ElectroDB (JS)pydynantic (Python) <pre><code>const { data } = await User.query\n  .primary({ orgId: \"acme\" })\n  .where(({ status }, { eq }) =&gt; eq(status, \"active\"))\n  .go({ limit: 50 });\n\nconst { data: byEmail } = await User.query.byEmail({ email: \"a@x.com\" }).go();\n</code></pre> <pre><code>from pydynantic import F\n\nusers = (User.query.primary(org_id=\"acme\")\n                   .filter(F(\"status\") == \"active\")\n                   .limit(50)\n                   .all())\n\nuser = User.query.by_email(email=\"a@x.com\").one_or_none()\n</code></pre>"},{"location":"migrating/from-electrodb/#whats-different","title":"What's different","text":"<ul> <li>No <code>.go()</code> / no async. <code>pydynantic</code> is synchronous; terminals like <code>.all()</code>,   <code>.one()</code>, <code>.page()</code> execute immediately. (Async is planned for a future major version.)</li> <li>Validation is Pydantic. Types, defaults, and nested models come from Pydantic v2   instead of ElectroDB's attribute schema.</li> <li>You inject the boto3 client on the <code>Table</code>; the library never   opens connections.</li> <li>Services \u2192 Collections. ElectroDB's <code>Service</code> for cross-entity queries maps to a   <code>Collection</code>. See Collections.</li> <li>No infra provisioning. Like ElectroDB, table creation is out of scope \u2014 use   CDK/Terraform.</li> </ul>"},{"location":"reference/api/","title":"API reference","text":"<p>Everything exported from the <code>pydynantic</code> top-level package. Each symbol below is generated directly from its docstring and signature.</p>"},{"location":"reference/api/#pydynantic","title":"pydynantic","text":"<p>pydynantic: single-table design for DynamoDB, with typed Pydantic entities.</p>"},{"location":"reference/api/#pydynantic.OperationHook","title":"OperationHook  <code>module-attribute</code>","text":"<pre><code>OperationHook = Callable[[OperationEvent], None]\n</code></pre> <p>A callable invoked once per DynamoDB operation; receives an :class:<code>OperationEvent</code>.</p>"},{"location":"reference/api/#pydynantic.Collection","title":"Collection","text":"<p>Base class for a set of entities sharing a partition.</p> <p>Declare <code>members = [User, Membership, Invoice]</code> on the subclass; all members must compose the same partition key. Query a partition with <code>OrgData.query(org_id=\"acme\").all()</code> and read results as <code>result.users</code>, <code>result.invoices</code> (the entity name pluralised).</p>"},{"location":"reference/api/#pydynantic.Collection.query","title":"query  <code>classmethod</code>","text":"<pre><code>query(*, attributes=None, **pk_attrs)\n</code></pre> <p>Query a partition for all member entity types.</p> <p><code>attributes</code> limits the response to a projection of attribute paths; omitted fields fall back to their model defaults (or fail validation if required), so include everything each member needs to construct. The discriminator attribute is always included so bucketing still works.</p>"},{"location":"reference/api/#pydynantic.CollectionPage","title":"CollectionPage  <code>dataclass</code>","text":"<p>A single page of a collection query plus a cursor for the next page.</p> <p><code>result</code> is a :class:<code>CollectionResult</code> bucketing the page's items by entity type; <code>cursor</code> is an opaque next-page token (<code>None</code> when the partition is drained).</p>"},{"location":"reference/api/#pydynantic.CollectionPage.has_more","title":"has_more  <code>property</code>","text":"<pre><code>has_more\n</code></pre> <p>Whether another page is available (a cursor was returned).</p>"},{"location":"reference/api/#pydynantic.CollectionResult","title":"CollectionResult","text":"<p>Holds the items of a collection query, bucketed per entity type.</p>"},{"location":"reference/api/#pydynantic.CollectionResult.all","title":"all","text":"<pre><code>all()\n</code></pre> <p>Every item across all buckets, in DynamoDB return order.</p>"},{"location":"reference/api/#pydynantic.Entity","title":"Entity","text":"<p>               Bases: <code>BaseModel</code></p> <p>Base class for single-table entities.</p> <p>Subclass with <code>class User(Entity, table=table, name=\"user\"): ...</code> and declare key templates on an inner <code>Meta</code> class via :func:<code>pydynantic.key</code>.</p>"},{"location":"reference/api/#pydynantic.Entity.to_dynamo","title":"to_dynamo","text":"<pre><code>to_dynamo()\n</code></pre> <p>Build the full DynamoDB item (attributes + composed keys + marker).</p>"},{"location":"reference/api/#pydynantic.Entity.from_dynamo","title":"from_dynamo  <code>classmethod</code>","text":"<pre><code>from_dynamo(item)\n</code></pre> <p>Deserialize a raw DynamoDB item into an entity instance.</p>"},{"location":"reference/api/#pydynantic.Entity.build_key","title":"build_key  <code>classmethod</code>","text":"<pre><code>build_key(attrs)\n</code></pre> <p>Build the primary-key portion of an item from attribute values.</p>"},{"location":"reference/api/#pydynantic.Entity.put","title":"put  <code>classmethod</code>","text":"<pre><code>put(item, *, condition=None, return_values='NONE')\n</code></pre> <p>Create or replace an item. Supports optimistic locking and conditions.</p> <p><code>put</code> always returns the written item (the object you passed in), so <code>put(x) is x</code>. <code>return_values=\"ALL_OLD\"</code> asks DynamoDB to surface the pre-write stored state at the API level, but <code>put</code>'s return value is unchanged \u2014 and with optimistic locking the returned object carries the bumped version. Only <code>NONE</code> and <code>ALL_OLD</code> are valid.</p>"},{"location":"reference/api/#pydynantic.Entity.get","title":"get  <code>classmethod</code>","text":"<pre><code>get(*, consistent=False, attributes=None, **key_attrs)\n</code></pre> <p>Fetch an item by its primary key, or <code>None</code> if it does not exist.</p> <p><code>attributes</code> limits the response to a projection of attribute paths; omitted fields fall back to their model defaults (or fail validation if required), so include everything the entity needs to construct.</p>"},{"location":"reference/api/#pydynantic.Entity.get_or_raise","title":"get_or_raise  <code>classmethod</code>","text":"<pre><code>get_or_raise(*, consistent=False, attributes=None, **key_attrs)\n</code></pre> <p>Like :meth:<code>get</code> but raises :class:<code>ItemNotFoundError</code> when missing.</p> <p>Accepts the same <code>consistent</code> and <code>attributes</code> read options as :meth:<code>get</code>.</p>"},{"location":"reference/api/#pydynantic.Entity.delete","title":"delete  <code>classmethod</code>","text":"<pre><code>delete(*, condition=None, return_values='NONE', **key_attrs)\n</code></pre> <p>Delete an item by its primary key.</p> <p>With <code>return_values=\"ALL_OLD\"</code> the deleted item is returned (or <code>None</code> if it did not exist); otherwise <code>delete</code> returns <code>None</code>. Only <code>NONE</code> and <code>ALL_OLD</code> are valid.</p>"},{"location":"reference/api/#pydynantic.Entity.update","title":"update  <code>classmethod</code>","text":"<pre><code>update(*, set=None, remove=None, add=None, delete=None, condition=None, expected_version=None, return_values='ALL_NEW', **key_attrs)\n</code></pre> <p>Apply a partial update via a generated <code>UpdateExpression</code>.</p> <p><code>set</code> assigns attributes, <code>remove</code> deletes them, <code>add</code> performs atomic numeric/set additions, <code>delete</code> removes elements from sets. Any GSI key whose source attributes change is recomputed automatically.</p> <p>On a versioned entity, pass <code>expected_version</code> to guard the write with the version you last read: a concurrent change raises :class:<code>~pydynantic.OptimisticLockError</code> instead of silently overwriting it. The stored <code>version</code> is always incremented regardless.</p>"},{"location":"reference/api/#pydynantic.Entity.batch_get","title":"batch_get  <code>classmethod</code>","text":"<pre><code>batch_get(keys, *, consistent=False, attributes=None)\n</code></pre> <p>Fetch many items by key, chunking and retrying transparently.</p> <p><code>attributes</code> limits the response to a projection of attribute paths; omitted fields fall back to their model defaults (or fail validation if required), so include everything the entity needs to construct.</p>"},{"location":"reference/api/#pydynantic.Entity.batch_write","title":"batch_write  <code>classmethod</code>","text":"<pre><code>batch_write(*, puts=None, deletes=None)\n</code></pre> <p>Write/delete many items, chunking and retrying unprocessed items.</p>"},{"location":"reference/api/#pydynantic.Entity.scan","title":"scan  <code>classmethod</code>","text":"<pre><code>scan()\n</code></pre> <p>Return a builder for a full-table scan restricted to this entity.</p> <p>A scan reads the whole table; the builder automatically filters on the <code>__entity__</code> discriminator so only this entity's items are returned. Prefer :attr:<code>query</code> whenever a key condition applies.</p>"},{"location":"reference/api/#pydynantic.ConditionCheckFailedError","title":"ConditionCheckFailedError","text":"<p>               Bases: <code>PydynanticError</code></p> <p>Raised when a write fails because its condition expression was not met.</p>"},{"location":"reference/api/#pydynantic.ItemNotFoundError","title":"ItemNotFoundError","text":"<p>               Bases: <code>PydynanticError</code></p> <p>Raised when a required item does not exist (e.g. <code>get</code>/<code>one</code>).</p>"},{"location":"reference/api/#pydynantic.KeyTemplateError","title":"KeyTemplateError","text":"<p>               Bases: <code>PydynanticError</code></p> <p>Raised when a key template references an attribute that is missing.</p>"},{"location":"reference/api/#pydynantic.MultipleResultsError","title":"MultipleResultsError","text":"<p>               Bases: <code>PydynanticError</code></p> <p>Raised by <code>one</code>/<code>one_or_none</code> when more than one item matches.</p>"},{"location":"reference/api/#pydynantic.OptimisticLockError","title":"OptimisticLockError","text":"<p>               Bases: <code>ConditionCheckFailedError</code></p> <p>Raised when a versioned write loses an optimistic-locking race.</p>"},{"location":"reference/api/#pydynantic.PydynanticError","title":"PydynanticError","text":"<p>               Bases: <code>Exception</code></p> <p>Base class for every error raised by pydynantic.</p>"},{"location":"reference/api/#pydynantic.TransactionCanceledError","title":"TransactionCanceledError","text":"<p>               Bases: <code>PydynanticError</code></p> <p>Raised when a <code>TransactWriteItems</code> call is cancelled.</p> <p>The per-action cancellation <code>reasons</code> (as returned by DynamoDB) are exposed on the <code>reasons</code> attribute for diagnostics.</p>"},{"location":"reference/api/#pydynantic.ValidationError","title":"ValidationError","text":"<p>               Bases: <code>PydynanticError</code></p> <p>Wraps a :class:<code>pydantic.ValidationError</code> for schema violations.</p>"},{"location":"reference/api/#pydynantic.Condition","title":"Condition","text":"<p>Base class for all expression nodes. Combine with <code>&amp;</code>, <code>|</code>, <code>~</code>.</p>"},{"location":"reference/api/#pydynantic.F","title":"F","text":"<p>Reference to an attribute, used to build :class:<code>Condition</code> objects.</p> <p>Supports dotted paths for nested map attributes, e.g. <code>F(\"address.city\")</code>.</p>"},{"location":"reference/api/#pydynantic.KeyDefinition","title":"KeyDefinition","text":"<p>A primary or secondary-index key declared on an entity's <code>Meta</code>.</p> <p>Instances are created by the :func:<code>key</code> factory and bound to a concrete :class:<code>~pydynantic.table.Table</code> (which resolves the physical attribute names) when the owning entity class is created.</p>"},{"location":"reference/api/#pydynantic.KeyDefinition.referenced_fields","title":"referenced_fields  <code>property</code>","text":"<pre><code>referenced_fields\n</code></pre> <p>All entity attributes this key reads, across its pk and sk templates.</p>"},{"location":"reference/api/#pydynantic.KeyDefinition.bind","title":"bind","text":"<pre><code>bind(name, table)\n</code></pre> <p>Resolve physical attribute names against the table configuration.</p>"},{"location":"reference/api/#pydynantic.KeyDefinition.build_pk","title":"build_pk","text":"<pre><code>build_pk(attrs)\n</code></pre> <p>Render the partition-key string from the given attribute values.</p>"},{"location":"reference/api/#pydynantic.KeyDefinition.build_sk","title":"build_sk","text":"<pre><code>build_sk(attrs)\n</code></pre> <p>Render the sort-key string, or <code>None</code> if this key has no sort key.</p>"},{"location":"reference/api/#pydynantic.KeyDefinition.key_attributes","title":"key_attributes","text":"<pre><code>key_attributes(attrs)\n</code></pre> <p>Return the physical key attributes (pk/sk) for an item.</p> <p>Returns an empty mapping if the required attributes for this key are not all present, which lets non-projected entities skip optional GSIs.</p>"},{"location":"reference/api/#pydynantic.OperationEvent","title":"OperationEvent  <code>dataclass</code>","text":"<p>A single DynamoDB operation, reported to an :data:<code>OperationHook</code>.</p>"},{"location":"reference/api/#pydynantic.Page","title":"Page  <code>dataclass</code>","text":"<p>               Bases: <code>Generic[T]</code></p> <p>A single page of query results plus a cursor for the next page.</p>"},{"location":"reference/api/#pydynantic.Page.has_more","title":"has_more  <code>property</code>","text":"<pre><code>has_more\n</code></pre> <p>Whether another page is available (a cursor was returned).</p>"},{"location":"reference/api/#pydynantic.QueryBuilder","title":"QueryBuilder","text":"<p>               Bases: <code>Generic[E]</code></p> <p>Fluent builder for a query against one declared key (primary or GSI).</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.eq","title":"eq","text":"<pre><code>eq(value)\n</code></pre> <p>Match items whose sort key equals <code>value</code>.</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.lt","title":"lt","text":"<pre><code>lt(value)\n</code></pre> <p>Match items whose sort key is less than <code>value</code>.</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.lte","title":"lte","text":"<pre><code>lte(value)\n</code></pre> <p>Match items whose sort key is less than or equal to <code>value</code>.</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.gt","title":"gt","text":"<pre><code>gt(value)\n</code></pre> <p>Match items whose sort key is greater than <code>value</code>.</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.gte","title":"gte","text":"<pre><code>gte(value)\n</code></pre> <p>Match items whose sort key is greater than or equal to <code>value</code>.</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.begins_with","title":"begins_with","text":"<pre><code>begins_with(prefix)\n</code></pre> <p>Match items whose sort key starts with <code>prefix</code>.</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.between","title":"between","text":"<pre><code>between(low, high)\n</code></pre> <p>Match items whose sort key falls within <code>[low, high]</code> inclusive.</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.filter","title":"filter","text":"<pre><code>filter(condition)\n</code></pre> <p>Add a post-read filter expression; chained calls are AND-combined.</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.limit","title":"limit","text":"<pre><code>limit(count)\n</code></pre> <p>Cap the number of items DynamoDB evaluates per page at <code>count</code>.</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.attributes","title":"attributes","text":"<pre><code>attributes(names)\n</code></pre> <p>Project only <code>names</code>; omitted fields fall back to model defaults.</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.ascending","title":"ascending","text":"<pre><code>ascending()\n</code></pre> <p>Return results in ascending sort-key order (the DynamoDB default).</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.descending","title":"descending","text":"<pre><code>descending()\n</code></pre> <p>Return results in descending sort-key order.</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.consistent","title":"consistent","text":"<pre><code>consistent(value=True)\n</code></pre> <p>Request a strongly consistent read. Mirrors <code>get(consistent=...)</code>.</p> <p>GSIs only support eventually consistent reads, so requesting a consistent read against an index is rejected eagerly.</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.iter","title":"iter","text":"<pre><code>iter()\n</code></pre> <p>Lazily iterate all matching items, paginating transparently.</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.all","title":"all","text":"<pre><code>all()\n</code></pre> <p>Return every matching item as a list.</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.first","title":"first","text":"<pre><code>first()\n</code></pre> <p>Return the first matching item, or <code>None</code>.</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.one_or_none","title":"one_or_none","text":"<pre><code>one_or_none()\n</code></pre> <p>Return the single match, <code>None</code> if none, error if more than one.</p> <p>Any user <code>.limit()</code> is ignored: this always probes for a second item so the \"multiple results\" case is never silently masked.</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.one","title":"one","text":"<pre><code>one()\n</code></pre> <p>Return the single match, raising if zero or more than one.</p> <p>Any user <code>.limit()</code> is ignored: this always probes for a second item so the \"multiple results\" case is never silently masked.</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.page","title":"page","text":"<pre><code>page(cursor=None)\n</code></pre> <p>Return a single page of results plus an opaque next-page cursor.</p>"},{"location":"reference/api/#pydynantic.QueryBuilder.count","title":"count","text":"<pre><code>count()\n</code></pre> <p>Count matching items server-side without materialising them.</p>"},{"location":"reference/api/#pydynantic.QueryNamespace","title":"QueryNamespace","text":"<p>               Bases: <code>Generic[E]</code></p> <p><code>Entity.query</code> accessor exposing one builder per declared key.</p>"},{"location":"reference/api/#pydynantic.ScanBuilder","title":"ScanBuilder","text":"<p>               Bases: <code>Generic[E]</code></p> <p>Fluent builder for a full-table scan restricted to one entity type.</p>"},{"location":"reference/api/#pydynantic.ScanBuilder.filter","title":"filter","text":"<pre><code>filter(condition)\n</code></pre> <p>Add a filter expression applied after the scan; chained calls AND-combine.</p>"},{"location":"reference/api/#pydynantic.ScanBuilder.limit","title":"limit","text":"<pre><code>limit(count)\n</code></pre> <p>Cap the number of items DynamoDB evaluates per page at <code>count</code>.</p>"},{"location":"reference/api/#pydynantic.ScanBuilder.attributes","title":"attributes","text":"<pre><code>attributes(names)\n</code></pre> <p>Project only <code>names</code>; omitted fields fall back to model defaults.</p>"},{"location":"reference/api/#pydynantic.ScanBuilder.iter","title":"iter","text":"<pre><code>iter()\n</code></pre> <p>Lazily iterate matching items, paginating transparently.</p>"},{"location":"reference/api/#pydynantic.ScanBuilder.all","title":"all","text":"<pre><code>all()\n</code></pre> <p>Return every matching item as a list.</p>"},{"location":"reference/api/#pydynantic.ScanBuilder.first","title":"first","text":"<pre><code>first()\n</code></pre> <p>Return the first matching item, or <code>None</code>.</p>"},{"location":"reference/api/#pydynantic.ScanBuilder.page","title":"page","text":"<pre><code>page(cursor=None)\n</code></pre> <p>Return a single page of results plus an opaque next-page cursor.</p>"},{"location":"reference/api/#pydynantic.ScanBuilder.count","title":"count","text":"<pre><code>count()\n</code></pre> <p>Count matching items server-side without materialising them.</p>"},{"location":"reference/api/#pydynantic.Index","title":"Index  <code>dataclass</code>","text":"<p>A secondary index's physical key attribute names.</p>"},{"location":"reference/api/#pydynantic.Table","title":"Table","text":"<p>Configuration for a single physical DynamoDB table.</p> <p>The boto3 client is injected (defaulting to <code>boto3.client(\"dynamodb\")</code>) so credentials, region and endpoint stay the caller's concern and tests can inject a mocked client.</p>"},{"location":"reference/api/#pydynantic.Table.register","title":"register","text":"<pre><code>register(entity_cls)\n</code></pre> <p>Register an entity so items can be routed back to it by <code>__entity__</code>.</p>"},{"location":"reference/api/#pydynantic.Table.entity_for","title":"entity_for","text":"<pre><code>entity_for(name)\n</code></pre> <p>Look up a registered entity class by its discriminator name.</p>"},{"location":"reference/api/#pydynantic.Transaction","title":"Transaction","text":"<p>Accumulates write actions and flushes them atomically on <code>commit</code>.</p>"},{"location":"reference/api/#pydynantic.Transaction.put","title":"put","text":"<pre><code>put(item, *, condition=None)\n</code></pre> <p>Queue a <code>Put</code> action.</p>"},{"location":"reference/api/#pydynantic.Transaction.delete","title":"delete","text":"<pre><code>delete(entity_cls, *, key, condition=None)\n</code></pre> <p>Queue a <code>Delete</code> action.</p>"},{"location":"reference/api/#pydynantic.Transaction.condition_check","title":"condition_check","text":"<pre><code>condition_check(entity_cls, *, key, condition)\n</code></pre> <p>Queue a <code>ConditionCheck</code> action (no write, just a guard).</p>"},{"location":"reference/api/#pydynantic.Transaction.update","title":"update","text":"<pre><code>update(entity_cls, *, key, set=None, remove=None, add=None, delete=None, condition=None)\n</code></pre> <p>Queue an <code>Update</code> action.</p>"},{"location":"reference/api/#pydynantic.Transaction.commit","title":"commit","text":"<pre><code>commit()\n</code></pre> <p>Send all queued actions as a single <code>TransactWriteItems</code> call.</p>"},{"location":"reference/api/#pydynantic.created_at_attr","title":"created_at_attr","text":"<pre><code>created_at_attr(**kwargs)\n</code></pre> <p>Mark a <code>datetime | None</code> field as the auto-managed creation timestamp.</p> <p>On the first <code>put</code> (when the field is still <code>None</code>) pydynantic stamps the field with <code>datetime.now(timezone.utc)</code>. Subsequent puts leave it untouched, and <code>update</code> never modifies it.</p>"},{"location":"reference/api/#pydynantic.ttl_attr","title":"ttl_attr","text":"<pre><code>ttl_attr(default=None, **kwargs)\n</code></pre> <p>Mark a field as the DynamoDB Time-To-Live (TTL) attribute.</p> <p>DynamoDB requires the TTL attribute to be a Number holding a Unix epoch timestamp in seconds. The field may be declared as a <code>datetime | None</code> (the common case) or an <code>int</code>; on every <code>put</code> the stored value is overridden to an epoch-seconds Number regardless of the model's default ISO datetime encoding. Reading back coerces the epoch into a <code>datetime</code> (UTC), so sub-second precision is dropped.</p> <p>The default is <code>None</code> (TTL is optional). When the value is <code>None</code> the attribute is omitted from the stored item entirely.</p> <p>Timezone: TTL <code>datetime</code> values should be timezone-aware. Naive datetimes are passed to <code>.timestamp()</code> unchanged (interpreted in the local timezone by Python); pydynantic does not reinterpret them, so prefer tz-aware values.</p>"},{"location":"reference/api/#pydynantic.updated_at_attr","title":"updated_at_attr","text":"<pre><code>updated_at_attr(**kwargs)\n</code></pre> <p>Mark a <code>datetime | None</code> field as the auto-managed modification timestamp.</p> <p>On every <code>put</code> the field is stamped with <code>datetime.now(timezone.utc)</code>. On <code>update</code> it is stamped too, unless the caller explicitly supplies it in <code>set=</code> (in which case the caller's value wins).</p>"},{"location":"reference/api/#pydynantic.version_attr","title":"version_attr","text":"<pre><code>version_attr(default=0, **kwargs)\n</code></pre> <p>Mark an integer field as the optimistic-locking version attribute.</p> <p>On every versioned <code>put</code>, the version is incremented and a condition is added requiring the stored version to match; a lost race raises :class:<code>~pydynantic.errors.OptimisticLockError</code>.</p>"},{"location":"reference/api/#pydynantic.attr_exists","title":"attr_exists","text":"<pre><code>attr_exists(path)\n</code></pre> <p>Convenience: <code>attribute_exists(path)</code>.</p>"},{"location":"reference/api/#pydynantic.attr_not_exists","title":"attr_not_exists","text":"<pre><code>attr_not_exists(path)\n</code></pre> <p>Convenience: <code>attribute_not_exists(path)</code> (e.g. create-only puts).</p>"},{"location":"reference/api/#pydynantic.key","title":"key","text":"<pre><code>key(*, pk, sk=None, index=None)\n</code></pre> <p>Declare a key template for an entity.</p> <p>:param pk: partition-key template, e.g. <code>\"ORG#{org_id}\"</code>. :param sk: optional sort-key template, e.g. <code>\"USER#{user_id}\"</code>. :param index: GSI name (from the table's <code>indexes</code>); <code>None</code> = primary.</p>"},{"location":"reference/api/#pydynantic.decode_cursor","title":"decode_cursor","text":"<pre><code>decode_cursor(cursor)\n</code></pre> <p>Decode an opaque cursor back into a DynamoDB <code>ExclusiveStartKey</code>.</p> <p><code>None</code> decodes to <code>None</code>. The cursor must be exactly the opaque, versioned token produced by :func:<code>encode_cursor</code> -- it is never hand-built. Any malformed, non-JSON, structurally-wrong or unsupported-version cursor raises :class:<code>~pydynantic.errors.PydynanticError</code> rather than leaking a raw decoding exception.</p>"},{"location":"reference/api/#pydynantic.encode_cursor","title":"encode_cursor","text":"<pre><code>encode_cursor(last_evaluated_key)\n</code></pre> <p>Encode a DynamoDB <code>LastEvaluatedKey</code> into an opaque cursor string.</p> <p>Returns a versioned, base64url-encoded token without <code>=</code> padding (HTTP/JSON-safe). <code>None</code> or an empty key yields <code>None</code>. The result is opaque: pass it back verbatim to resume pagination; do not parse or build one by hand.</p>"},{"location":"reference/api/#pydynantic.transaction","title":"transaction","text":"<pre><code>transaction(table)\n</code></pre> <p>Context manager that commits queued actions on a clean exit.</p> <p>If the <code>with</code> block raises, <code>commit</code> is never reached, so no actions are sent (rollback). If a queued condition fails at commit time, DynamoDB cancels the whole transaction atomically.</p>"},{"location":"reference/api/#re-exported-from-pydantic","title":"Re-exported from Pydantic","text":"<p><code>pydynantic</code> re-exports Pydantic's <code>Field</code> for convenience, so you can declare entity attribute metadata without a second import. It behaves exactly like <code>pydantic.Field</code>; see the linked Pydantic docs for every parameter.</p>"},{"location":"reference/api/#pydantic.Field","title":"pydantic.Field","text":"<pre><code>Field(default: ellipsis, *, alias: str | None = _Unset, alias_priority: int | None = _Unset, validation_alias: str | AliasPath | AliasChoices | None = _Unset, serialization_alias: str | None = _Unset, title: str | None = _Unset, field_title_generator: Callable[[str, FieldInfo], str] | None = _Unset, description: str | None = _Unset, examples: list[Any] | None = _Unset, exclude: bool | None = _Unset, exclude_if: Callable[[Any], bool] | None = _Unset, discriminator: str | Discriminator | None = _Unset, deprecated: Deprecated | str | bool | None = _Unset, json_schema_extra: JsonDict | Callable[[JsonDict], None] | None = _Unset, frozen: bool | None = _Unset, validate_default: bool | None = _Unset, repr: bool = _Unset, init: bool | None = _Unset, init_var: bool | None = _Unset, kw_only: bool | None = _Unset, pattern: str | Pattern[str] | None = _Unset, strict: bool | None = _Unset, coerce_numbers_to_str: bool | None = _Unset, gt: SupportsGt | None = _Unset, ge: SupportsGe | None = _Unset, lt: SupportsLt | None = _Unset, le: SupportsLe | None = _Unset, multiple_of: float | None = _Unset, allow_inf_nan: bool | None = _Unset, max_digits: int | None = _Unset, decimal_places: int | None = _Unset, min_length: int | None = _Unset, max_length: int | None = _Unset, union_mode: Literal['smart', 'left_to_right'] = _Unset, fail_fast: bool | None = _Unset, **extra: Unpack[_EmptyKwargs]) -&gt; Any\n</code></pre><pre><code>Field(default: Any, *, alias: str | None = _Unset, alias_priority: int | None = _Unset, validation_alias: str | AliasPath | AliasChoices | None = _Unset, serialization_alias: str | None = _Unset, title: str | None = _Unset, field_title_generator: Callable[[str, FieldInfo], str] | None = _Unset, description: str | None = _Unset, examples: list[Any] | None = _Unset, exclude: bool | None = _Unset, exclude_if: Callable[[Any], bool] | None = _Unset, discriminator: str | Discriminator | None = _Unset, deprecated: Deprecated | str | bool | None = _Unset, json_schema_extra: JsonDict | Callable[[JsonDict], None] | None = _Unset, frozen: bool | None = _Unset, validate_default: Literal[True], repr: bool = _Unset, init: bool | None = _Unset, init_var: bool | None = _Unset, kw_only: bool | None = _Unset, pattern: str | Pattern[str] | None = _Unset, strict: bool | None = _Unset, coerce_numbers_to_str: bool | None = _Unset, gt: SupportsGt | None = _Unset, ge: SupportsGe | None = _Unset, lt: SupportsLt | None = _Unset, le: SupportsLe | None = _Unset, multiple_of: float | None = _Unset, allow_inf_nan: bool | None = _Unset, max_digits: int | None = _Unset, decimal_places: int | None = _Unset, min_length: int | None = _Unset, max_length: int | None = _Unset, union_mode: Literal['smart', 'left_to_right'] = _Unset, fail_fast: bool | None = _Unset, **extra: Unpack[_EmptyKwargs]) -&gt; Any\n</code></pre><pre><code>Field(default: _T, *, alias: str | None = _Unset, alias_priority: int | None = _Unset, validation_alias: str | AliasPath | AliasChoices | None = _Unset, serialization_alias: str | None = _Unset, title: str | None = _Unset, field_title_generator: Callable[[str, FieldInfo], str] | None = _Unset, description: str | None = _Unset, examples: list[Any] | None = _Unset, exclude: bool | None = _Unset, exclude_if: Callable[[Any], bool] | None = _Unset, discriminator: str | Discriminator | None = _Unset, deprecated: Deprecated | str | bool | None = _Unset, json_schema_extra: JsonDict | Callable[[JsonDict], None] | None = _Unset, frozen: bool | None = _Unset, validate_default: Literal[False] = ..., repr: bool = _Unset, init: bool | None = _Unset, init_var: bool | None = _Unset, kw_only: bool | None = _Unset, pattern: str | Pattern[str] | None = _Unset, strict: bool | None = _Unset, coerce_numbers_to_str: bool | None = _Unset, gt: SupportsGt | None = _Unset, ge: SupportsGe | None = _Unset, lt: SupportsLt | None = _Unset, le: SupportsLe | None = _Unset, multiple_of: float | None = _Unset, allow_inf_nan: bool | None = _Unset, max_digits: int | None = _Unset, decimal_places: int | None = _Unset, min_length: int | None = _Unset, max_length: int | None = _Unset, union_mode: Literal['smart', 'left_to_right'] = _Unset, fail_fast: bool | None = _Unset, **extra: Unpack[_EmptyKwargs]) -&gt; _T\n</code></pre><pre><code>Field(*, default_factory: Callable[[], Any] | Callable[[dict[str, Any]], Any], alias: str | None = _Unset, alias_priority: int | None = _Unset, validation_alias: str | AliasPath | AliasChoices | None = _Unset, serialization_alias: str | None = _Unset, title: str | None = _Unset, field_title_generator: Callable[[str, FieldInfo], str] | None = _Unset, description: str | None = _Unset, examples: list[Any] | None = _Unset, exclude: bool | None = _Unset, exclude_if: Callable[[Any], bool] | None = _Unset, discriminator: str | Discriminator | None = _Unset, deprecated: Deprecated | str | bool | None = _Unset, json_schema_extra: JsonDict | Callable[[JsonDict], None] | None = _Unset, frozen: bool | None = _Unset, validate_default: Literal[True], repr: bool = _Unset, init: bool | None = _Unset, init_var: bool | None = _Unset, kw_only: bool | None = _Unset, pattern: str | Pattern[str] | None = _Unset, strict: bool | None = _Unset, coerce_numbers_to_str: bool | None = _Unset, gt: SupportsGt | None = _Unset, ge: SupportsGe | None = _Unset, lt: SupportsLt | None = _Unset, le: SupportsLe | None = _Unset, multiple_of: float | None = _Unset, allow_inf_nan: bool | None = _Unset, max_digits: int | None = _Unset, decimal_places: int | None = _Unset, min_length: int | None = _Unset, max_length: int | None = _Unset, union_mode: Literal['smart', 'left_to_right'] = _Unset, fail_fast: bool | None = _Unset, **extra: Unpack[_EmptyKwargs]) -&gt; Any\n</code></pre><pre><code>Field(*, default_factory: Callable[[], _T] | Callable[[dict[str, Any]], _T], alias: str | None = _Unset, alias_priority: int | None = _Unset, validation_alias: str | AliasPath | AliasChoices | None = _Unset, serialization_alias: str | None = _Unset, title: str | None = _Unset, field_title_generator: Callable[[str, FieldInfo], str] | None = _Unset, description: str | None = _Unset, examples: list[Any] | None = _Unset, exclude: bool | None = _Unset, exclude_if: Callable[[Any], bool] | None = _Unset, discriminator: str | Discriminator | None = _Unset, deprecated: Deprecated | str | bool | None = _Unset, json_schema_extra: JsonDict | Callable[[JsonDict], None] | None = _Unset, frozen: bool | None = _Unset, validate_default: Literal[False] | None = _Unset, repr: bool = _Unset, init: bool | None = _Unset, init_var: bool | None = _Unset, kw_only: bool | None = _Unset, pattern: str | Pattern[str] | None = _Unset, strict: bool | None = _Unset, coerce_numbers_to_str: bool | None = _Unset, gt: SupportsGt | None = _Unset, ge: SupportsGe | None = _Unset, lt: SupportsLt | None = _Unset, le: SupportsLe | None = _Unset, multiple_of: float | None = _Unset, allow_inf_nan: bool | None = _Unset, max_digits: int | None = _Unset, decimal_places: int | None = _Unset, min_length: int | None = _Unset, max_length: int | None = _Unset, union_mode: Literal['smart', 'left_to_right'] = _Unset, fail_fast: bool | None = _Unset, **extra: Unpack[_EmptyKwargs]) -&gt; _T\n</code></pre><pre><code>Field(*, alias: str | None = _Unset, alias_priority: int | None = _Unset, validation_alias: str | AliasPath | AliasChoices | None = _Unset, serialization_alias: str | None = _Unset, title: str | None = _Unset, field_title_generator: Callable[[str, FieldInfo], str] | None = _Unset, description: str | None = _Unset, examples: list[Any] | None = _Unset, exclude: bool | None = _Unset, exclude_if: Callable[[Any], bool] | None = _Unset, discriminator: str | Discriminator | None = _Unset, deprecated: Deprecated | str | bool | None = _Unset, json_schema_extra: JsonDict | Callable[[JsonDict], None] | None = _Unset, frozen: bool | None = _Unset, validate_default: bool | None = _Unset, repr: bool = _Unset, init: bool | None = _Unset, init_var: bool | None = _Unset, kw_only: bool | None = _Unset, pattern: str | Pattern[str] | None = _Unset, strict: bool | None = _Unset, coerce_numbers_to_str: bool | None = _Unset, gt: SupportsGt | None = _Unset, ge: SupportsGe | None = _Unset, lt: SupportsLt | None = _Unset, le: SupportsLe | None = _Unset, multiple_of: float | None = _Unset, allow_inf_nan: bool | None = _Unset, max_digits: int | None = _Unset, decimal_places: int | None = _Unset, min_length: int | None = _Unset, max_length: int | None = _Unset, union_mode: Literal['smart', 'left_to_right'] = _Unset, fail_fast: bool | None = _Unset, **extra: Unpack[_EmptyKwargs]) -&gt; Any\n</code></pre> <pre><code>Field(default=PydanticUndefined, *, default_factory=_Unset, alias=_Unset, alias_priority=_Unset, validation_alias=_Unset, serialization_alias=_Unset, title=_Unset, field_title_generator=_Unset, description=_Unset, examples=_Unset, exclude=_Unset, exclude_if=_Unset, discriminator=_Unset, deprecated=_Unset, json_schema_extra=_Unset, frozen=_Unset, validate_default=_Unset, repr=_Unset, init=_Unset, init_var=_Unset, kw_only=_Unset, pattern=_Unset, strict=_Unset, coerce_numbers_to_str=_Unset, gt=_Unset, ge=_Unset, lt=_Unset, le=_Unset, multiple_of=_Unset, allow_inf_nan=_Unset, max_digits=_Unset, decimal_places=_Unset, min_length=_Unset, max_length=_Unset, union_mode=_Unset, fail_fast=_Unset, **extra)\n</code></pre>"}]}